// Expanded Question Bank - 50 questions per domain with variations
export const domains = [
    {
        id: 'sde-frontend',
        title: 'Frontend Developer',
        description: 'React, JavaScript, CSS, and Web Performance.',
        icon: 'Layout',
        questions: [
            { text: "Explain the Virtual DOM and how it improves performance.", ideal: "The Virtual DOM is a lightweight, in-memory representation of the real DOM. When state changes, React updates the Virtual DOM first, compares it with the previous version using a diffing algorithm, and then selectively updates only the changed elements in the real DOM through reconciliation. This minimizes expensive direct DOM manipulations, reduces reflows and repaints, and allows React to batch multiple updates together for better performance." },
            { text: "How does React's Virtual DOM work under the hood?", ideal: "Under the hood, React maintains a virtual tree structure in JavaScript memory. When a component's state changes, React creates a new virtual tree, performs a diffing algorithm to identify what changed by comparing the new tree with the previous one, and then uses a process called reconciliation to apply only the minimal necessary changes to the actual DOM. This involves three main steps: (1) Create new virtual DOM tree, (2) Diff with previous virtual DOM, (3) Patch real DOM with minimal changes using efficient algorithms." },
            { text: "What performance benefits does the Virtual DOM provide?", ideal: "The Virtual DOM provides several key performance benefits: (1) Batch updates - multiple state changes are collected and applied in a single DOM update cycle, (2) Selective rendering - only changed elements are updated, not the entire DOM tree, (3) Efficient diffing - React's reconciliation algorithm identifies minimal changes needed using heuristics, (4) Reduced reflows and repaints - fewer direct DOM manipulations mean less browser rendering work, (5) Predictable performance - developers don't need to manually optimize DOM updates. This makes React applications significantly faster than naive jQuery-style DOM manipulation." },
            { text: "What is the difference between local and global state?", ideal: "Local state (component state) is managed within a single component using hooks like useState and is only accessible to that component and its children via props. It's ideal for UI-specific data like form inputs, toggles, or temporary values. Global state is shared across multiple components throughout the application using solutions like Redux, Context API, or Zustand. It's used for data that many components need access to, such as user authentication, theme preferences, or shopping cart contents. The key difference is scope and accessibility." },
            { text: "When would you use Redux versus Context API?", ideal: "Use Redux when you need: (1) Complex state logic with multiple actions and reducers, (2) Middleware for side effects (Redux Thunk/Saga), (3) Time-travel debugging with Redux DevTools, (4) Highly predictable state updates, (5) Large applications with many global state updates. Use Context API when you need: (1) Simple global state like theme or user auth, (2) Avoiding prop drilling for a few values, (3) Smaller applications, (4) Less boilerplate code. Redux has more overhead but provides better structure and tooling for complex state management." },
            { text: "How do you decide between component state and global state?", ideal: "Use component state (useState) when: (1) Data is only needed by that component and its direct children, (2) Data is UI-specific like form inputs or modal visibility, (3) Data doesn't need to persist across component unmounts. Use global state when: (1) Multiple unrelated components need the same data, (2) You're experiencing prop drilling (passing props through many levels), (3) Data needs to persist across route changes, (4) Multiple components need to update the same data. Start with local state and lift it up or move to global state only when necessary." },
            { text: "Explain the useEffect hook dependency array.", ideal: "The dependency array in useEffect controls when the effect runs. If the array is empty [], the effect runs only once after the initial render (component mount). If it contains values like [count, user], the effect runs whenever any of those values change. If the dependency array is omitted entirely, the effect runs after every render. You should include all values from component scope that the effect uses. The cleanup function returned from useEffect runs before the effect re-runs and when the component unmounts, preventing memory leaks from subscriptions or timers." },
            { text: "How does the dependency array in useEffect control execution?", ideal: "The dependency array acts as a trigger mechanism for useEffect. React compares the current values in the dependency array with the previous values using shallow comparison (Object.is). If any value has changed, React runs the effect. This allows you to control exactly when side effects execute. For example, [userId] means 'run this effect only when userId changes', while [] means 'run only on mount'. This prevents unnecessary effect executions and allows you to synchronize your component with external systems efficiently." },
            { text: "What happens if you omit the dependency array in useEffect?", ideal: "If you omit the dependency array entirely (just useEffect(() => {...})), the effect runs after every single render of the component. This can cause performance issues and infinite loops if the effect triggers a state update, which causes a re-render, which runs the effect again. This is rarely what you want. It's almost always better to use an empty array [] for mount-only effects or specify exact dependencies. Omitting the array is usually a mistake unless you specifically need the effect to run on every render." },
            { text: "What are Higher-Order Components (HOCs)?", ideal: "Higher-Order Components are functions that take a component as input and return a new enhanced component with additional props or behavior. They follow the pattern: const EnhancedComponent = higherOrderComponent(WrappedComponent). HOCs are used for cross-cutting concerns like authentication, logging, or data fetching. For example, withAuth(ProfilePage) could check if a user is logged in before rendering ProfilePage. HOCs enable code reuse without modifying the original component, though they've been largely replaced by hooks in modern React." },
            { text: "Can you give an example of a Higher-Order Component?", ideal: "A common HOC example is withAuth for protecting routes: const withAuth = (Component) => { return (props) => { const { isAuthenticated } = useAuth(); if (!isAuthenticated) return <Navigate to='/login' />; return <Component {...props} />; }; }. You'd use it like: const ProtectedProfile = withAuth(ProfilePage). Another example is withLoading: const withLoading = (Component) => ({ isLoading, ...props }) => isLoading ? <Spinner /> : <Component {...props} />. HOCs wrap components to add reusable logic without modifying the original component code." },
            { text: "How do you optimize a React application that renders slowly?", ideal: "To optimize slow React apps: (1) Use React.memo() to prevent unnecessary re-renders of functional components when props haven't changed, (2) Use useMemo() for expensive calculations so they're only recomputed when dependencies change, (3) Use useCallback() to memoize function references passed to child components, (4) Implement code splitting with React.lazy() and Suspense to load components on demand, (5) Virtualize long lists with react-window or react-virtualized to render only visible items, (6) Optimize images (lazy loading, proper formats, compression), (7) Use the React DevTools Profiler to identify slow components, (8) Avoid inline object/array creation in render, (9) Debounce expensive operations like API calls." },
            { text: "What techniques can improve React app performance?", ideal: "Key performance techniques include: (1) Component memoization - React.memo, useMemo, useCallback to prevent unnecessary work, (2) Code splitting - lazy load routes and heavy components with React.lazy(), (3) List virtualization - render only visible items in long lists using libraries like react-window, (4) Proper key usage - stable, unique keys for list items to help React's reconciliation, (5) State management - keep state as local as possible, avoid unnecessary global state, (6) Bundle optimization - tree shaking, minification, compression, (7) Image optimization - WebP format, lazy loading, responsive images, (8) Avoid anonymous functions in JSX - use useCallback for event handlers, (9) Production builds - always deploy optimized production builds." },
            { text: "Explain the concept of Lifting State Up.", ideal: "Lifting State Up is a pattern where you move state from child components to their closest common ancestor when multiple components need to share and synchronize the same data. For example, if two sibling components need to display and update the same value, you lift the state to their parent component and pass it down via props. The parent manages the state with useState, passes the current value as a prop, and passes an update function as a prop. This ensures a single source of truth and keeps components synchronized." },
            { text: "When should you lift state up in React?", ideal: "You should lift state up when: (1) Two or more sibling components need to access the same state, (2) Components need to stay synchronized with the same changing data, (3) A child component's state needs to be accessed by its parent or siblings, (4) You want a single source of truth for shared data. For example, if you have a temperature input in Celsius and Fahrenheit that need to stay in sync, lift the temperature state to their common parent. Don't lift state unnecessarily - keep it as local as possible until sharing is actually needed." },
            { text: "What is the difference between useMemo and useCallback?", ideal: "useMemo memoizes the result value of a function - it caches the computed value and only recalculates when dependencies change. Use it for expensive calculations: const expensiveValue = useMemo(() => computeExpensiveValue(a, b), [a, b]). useCallback memoizes the function itself - it returns the same function instance across renders unless dependencies change. Use it to prevent child re-renders: const handleClick = useCallback(() => doSomething(id), [id]). In short: useMemo returns a memoized value, useCallback returns a memoized function. Both help optimize performance by preventing unnecessary recalculations or re-renders." },
            { text: "When would you use useMemo versus useCallback?", ideal: "Use useMemo when you have expensive computations that don't need to run on every render, like filtering/sorting large arrays, complex calculations, or transforming data: const sortedList = useMemo(() => items.sort(compareFn), [items]). Use useCallback when passing functions as props to child components wrapped in React.memo, to prevent the child from re-rendering unnecessarily: const handleSubmit = useCallback(() => submitForm(data), [data]). Remember: useCallback(fn, deps) is equivalent to useMemo(() => fn, deps). Only use these hooks when you have actual performance issues - premature optimization adds complexity." },
            { text: "How does React handle events differently than standard HTML?", ideal: "React uses Synthetic Events, which are cross-browser wrappers around native browser events. Key differences: (1) Event names are camelCase (onClick not onclick), (2) You pass functions not strings (onClick={handleClick} not onclick='handleClick()'), (3) You can't return false to prevent default - must call e.preventDefault() explicitly, (4) Events are pooled and reused for performance (though this changed in React 17), (5) React uses event delegation - attaches a single listener at the root instead of individual elements, improving performance. Synthetic Events have the same interface as native events (stopPropagation, preventDefault) but work consistently across all browsers." },
            { text: "What are Synthetic Events in React?", ideal: "Synthetic Events are React's cross-browser wrapper around the browser's native event system. They provide a consistent API across all browsers, normalizing differences in event handling between Chrome, Firefox, Safari, etc. React implements event delegation by attaching a single event listener to the document root (or root container in React 17+) rather than individual DOM nodes, which improves performance. Synthetic Events are pooled and reused - the event object is nullified after the callback runs (though this changed in React 17). They have the same interface as native events (target, currentTarget, preventDefault, stopPropagation) but ensure consistent behavior everywhere." },
            { text: "Explain server-side rendering versus client-side rendering.", ideal: "Client-Side Rendering (CSR): The browser downloads a minimal HTML file, then JavaScript runs to build the entire UI. Pros: Rich interactions, faster subsequent navigation, less server load. Cons: Slower initial load, poor SEO (though improved with modern crawlers), blank screen while JS loads. Server-Side Rendering (SSR): The server generates full HTML for each request and sends it ready-to-display. Pros: Faster initial page load, better SEO, works without JavaScript. Cons: Slower navigation between pages, higher server load, more complex setup. Modern frameworks like Next.js offer hybrid approaches with SSR for initial load and CSR for navigation." },
            { text: "What are the benefits of server-side rendering?", ideal: "SSR benefits include: (1) Faster First Contentful Paint - users see content immediately without waiting for JavaScript to download and execute, (2) Better SEO - search engines receive fully rendered HTML, making content easily indexable, (3) Improved performance on slow devices - less JavaScript processing on the client, (4) Better social media sharing - meta tags are rendered server-side for proper preview cards, (5) Works without JavaScript - basic functionality available even if JS fails or is disabled, (6) Faster time-to-interactive for initial page load. Frameworks like Next.js make SSR easy to implement while maintaining the benefits of React's component model." },
            { text: "What are React Portals and when would you use them?", ideal: "React Portals provide a way to render children into a DOM node that exists outside the parent component's DOM hierarchy using ReactDOM.createPortal(child, container). This is useful for modals, tooltips, dropdowns, or any UI that needs to visually break out of its container while maintaining React's event bubbling and context. For example, a modal rendered deep in the component tree can portal to document.body to avoid z-index and overflow issues, while still receiving props and context from its React parent." },
            { text: "How do you create modals that render outside the component tree?", ideal: "Use ReactDOM.createPortal() to render modals outside the normal DOM hierarchy: const Modal = ({ children }) => { return ReactDOM.createPortal(<div className='modal-overlay'>{children}</div>, document.getElementById('modal-root')); }. First, add a <div id='modal-root'></div> to your index.html. The portal renders the modal at the root level, avoiding CSS issues like z-index stacking and overflow:hidden from parent containers, while the component still behaves like a normal React child (receives props, context, and events bubble up the React tree)." },
            { text: "Explain the difference between var, let, and const.", ideal: "var is function-scoped, hoisted to the top of its function, and can be redeclared, which often causes bugs. let and const are block-scoped (only exist within {}), not hoisted in a usable way (temporal dead zone), and cannot be redeclared. const creates an immutable binding - you can't reassign the variable, but objects/arrays referenced by const can still be mutated. Best practice: always use const by default, only use let when you need to reassign, never use var in modern JavaScript." },
            { text: "What is block scoping in JavaScript?", ideal: "Block scoping means variables are only accessible within the block {} where they're defined. let and const are block-scoped - they only exist within if statements, for loops, or any {} block. var is function-scoped, not block-scoped - it's accessible throughout the entire function regardless of blocks. Example: if (true) { let x = 1; var y = 2; } - x is not accessible outside the if block, but y is. Block scoping prevents variable leakage and makes code more predictable." },
            { text: "What is a Closure in JavaScript?", ideal: "A closure is a function that has access to variables from its outer (enclosing) function's scope, even after the outer function has returned. This happens because JavaScript functions form closures over their lexical environment. Example: function outer() { const name = 'John'; return function inner() { console.log(name); }; }. The inner function 'closes over' the name variable and can access it even after outer() has finished executing. Closures are used for data privacy, creating function factories, and maintaining state in callbacks." },
            { text: "Can you provide a practical use case for closures?", ideal: "A practical closure example is a counter with private state: function createCounter() { let count = 0; return { increment: () => ++count, decrement: () => --count, getCount: () => count }; }. const counter = createCounter(); counter.increment(); // 1. The count variable is private - only accessible through the returned methods. Each call to createCounter() creates a new independent closure with its own count. This pattern is used for data encapsulation, module patterns, and maintaining private state without classes." },
            { text: "Explain the Event Loop, Microtasks, and Macrotasks.", ideal: "The event loop has two queues: Microtasks (Promise callbacks, queueMicrotask) and Macrotasks (setTimeout, setInterval, I/O). After each macrotask, the event loop processes ALL microtasks before moving to the next macrotask. This means Promises always execute before setTimeout, even if setTimeout has a 0ms delay. Example: setTimeout(() => console.log('macro'), 0); Promise.resolve().then(() => console.log('micro')); logs 'micro' first. This priority system ensures Promise chains complete before other async operations." },
            { text: "How does JavaScript handle asynchronous operations?", ideal: "JavaScript handles async operations using the event loop, callback queue, and Web APIs. When you call setTimeout, fetch, or addEventListener, the browser's Web APIs handle the operation outside the JavaScript thread. When the operation completes, its callback is added to the callback queue (or microtask queue for Promises). The event loop continuously checks if the call stack is empty - if it is, it moves callbacks from the queue to the stack for execution. This allows JavaScript to remain single-threaded while handling multiple async operations. Promises/async-await provide a cleaner syntax for this pattern than raw callbacks." },
            { text: "What is the difference between == and === in JavaScript?", ideal: "== performs type coercion before comparison (loose equality). === checks both value and type (strict equality). Always use === to avoid unexpected bugs from type coercion." },
            { text: "Explain promises and async/await in JavaScript.", ideal: "Promises represent the eventual completion (or failure) of an asynchronous operation. A Promise has three states: pending, fulfilled, or rejected. async/await is syntactic sugar that makes Promises easier to work with. An async function always returns a Promise. The await keyword pauses execution until the Promise resolves. This eliminates .then() chains: instead of fetch(url).then(r => r.json()).then(data => {...}), you write: const response = await fetch(url); const data = await response.json();. It improves readability and makes error handling easier with try/catch." },
            { text: "How do you handle errors in async/await?", ideal: "Handle errors in async/await using try/catch blocks: async function fetchData() { try { const response = await fetch(url); const data = await response.json(); return data; } catch (error) { console.error('Failed:', error); throw error; } }. The catch block handles any rejection in the await chain. You can also use .catch() on the async function call: fetchData().catch(handleError). For multiple independent promises, use Promise.allSettled() to handle both successes and failures. Always add error handling to prevent unhandled promise rejections." },
            { text: "What is event delegation in JavaScript?", ideal: "Event delegation attaches a single event listener to a parent element instead of individual children. It uses event bubbling to handle events from child elements, improving performance and handling dynamic content." },
            { text: "Explain the CSS Box Model.", ideal: "The box model consists of: Content, Padding (space inside), Border, and Margin (space outside). box-sizing: border-box includes padding and border in width/height calculations." },
            { text: "What is the difference between display: none and visibility: hidden?", ideal: "display: none removes the element from the document flow (no space). visibility: hidden hides the element but maintains its space in the layout." },
            { text: "Explain Flexbox and Grid. When would you use each?", ideal: "Flexbox is for 1D layouts (row or column). Grid is for 2D layouts (rows and columns simultaneously). Use Flexbox for navigation bars, Grid for page layouts." },
            { text: "What are CSS preprocessors like SASS/SCSS?", ideal: "Preprocessors extend CSS with variables, nesting, mixins, and functions. They compile to regular CSS. Benefits: Code reusability, better organization, and maintainability." },
            { text: "How do you implement responsive design?", ideal: "Use media queries, flexible grids, flexible images, and mobile-first approach. CSS frameworks like Tailwind have built-in responsive utilities (sm:, md:, lg:)." },
            { text: "What is debouncing and throttling?", ideal: "Debouncing delays function execution until after a pause in events (search input). Throttling limits function execution to once per time period (scroll events). Both optimize performance." },
            { text: "Explain the this keyword in JavaScript.", ideal: "this refers to the context object. In methods, it's the object. In functions, it's window/undefined (strict). Arrow functions inherit this from parent scope." },
            { text: "What are Web Workers and when would you use them?", ideal: "Web Workers run JavaScript in background threads, preventing UI blocking for heavy computations. Use for: data processing, image manipulation, or complex calculations." },
            { text: "Explain localStorage, sessionStorage, and cookies.", ideal: "localStorage persists data indefinitely. sessionStorage clears on tab close. Cookies are sent with every HTTP request and can be set with expiration. Use localStorage for user preferences." },
            { text: "What is Cross-Site Scripting (XSS) and how do you prevent it?", ideal: "XSS is injecting malicious scripts into web pages. Prevent by: sanitizing user input, using Content Security Policy, escaping HTML, and using frameworks that auto-escape (React)." },
            { text: "Explain the critical rendering path.", ideal: "The browser constructs DOM from HTML, CSSOM from CSS, combines them into Render Tree, calculates layout, and paints pixels. Optimize by minimizing resources, deferring scripts, and inlining critical CSS." },
            { text: "What are Progressive Web Apps (PWAs)?", ideal: "PWAs are web apps that behave like native apps. They use Service Workers for offline functionality, Web App Manifest for installation, and HTTPS for security." },
            { text: "How do you optimize website performance?", ideal: "Optimize images, minify CSS/JS, use code splitting, lazy loading, CDN, caching strategies, reduce HTTP requests, and use performance monitoring tools." },
            { text: "What is tree shaking in webpack?", ideal: "Tree shaking removes unused code from the final bundle. It works with ES6 modules. Configure webpack in production mode and use side-effect-free code for best results." },
            { text: "Explain the concept of controlled vs uncontrolled components.", ideal: "Controlled components have form data handled by React state (value + onChange). Uncontrolled use DOM refs to access values. Controlled components are preferred for validation and dynamic behavior." },
            { text: "What is the purpose of keys in React lists?", ideal: "Keys help React identify which items changed, were added, or removed. Use stable, unique IDs (not random). Keys improve reconciliation performance and prevent rendering bugs." },
            { text: "How does React Fiber improve React?", ideal: "Fiber is React's reconciliation algorithm rewrite. It enables incremental rendering, pausing work to handle high-priority updates, and better error boundaries with component stack traces." }
        ]
    },
    {
        id: 'sde-backend',
        title: 'Backend Developer',
        description: 'Python, Java, Node.js, Databases, and System Design.',
        icon: 'Code',
        questions: [
            { text: "Explain the difference between a list and a tuple in Python.", ideal: "Lists are mutable - you can add, remove, or modify elements after creation using methods like append(), remove(), or indexing. Tuples are immutable - once created, you cannot change their contents. Lists are defined with [], tuples with (). Because tuples are immutable, they're faster and use less memory than lists. Lists are used when you need a dynamic collection that changes, tuples for fixed collections like coordinates (x, y) or database records. Tuples can be used as dictionary keys, lists cannot." },
            { text: "When would you use a tuple instead of a list?", ideal: "Use tuples when: (1) Data should not change (coordinates, RGB values, database records), (2) You need to use the collection as a dictionary key (tuples are hashable, lists aren't), (3) You want to return multiple values from a function, (4) Performance matters - tuples are faster and use less memory, (5) You want to protect data from accidental modification. Use lists when you need to add/remove elements or modify the collection. Tuples communicate intent - this data is meant to be constant." },
            { text: "What are Python Decorators and how do they work?", ideal: "Decorators are functions that wrap other functions to modify their behavior without changing their code. They use the @ syntax. A decorator takes a function as input, adds functionality, and returns a modified function. Example: def timer(func): def wrapper(*args, **kwargs): start = time.time(); result = func(*args, **kwargs); print(f'Took {time.time()-start}s'); return result; return wrapper. Then @timer above any function will time its execution. Decorators are used for logging, authentication, caching, and validation." },
            { text: "Can you give an example of using decorators in Python?", ideal: "A practical decorator example for authentication: def login_required(func): def wrapper(*args, **kwargs): if not current_user.is_authenticated: return redirect('/login'); return func(*args, **kwargs); return wrapper. Use it like: @login_required def dashboard(): return render_template('dashboard.html'). Another example is caching: @lru_cache(maxsize=128) def fibonacci(n): return n if n < 2 else fibonacci(n-1) + fibonacci(n-2). The decorator automatically caches results. Decorators can also take parameters: @app.route('/users', methods=['GET', 'POST'])." },
            { text: "Explain the Global Interpreter Lock (GIL).", ideal: "The GIL is a mutex (lock) in CPython that allows only one thread to execute Python bytecode at a time, even on multi-core systems. This prevents race conditions in memory management but makes CPU-bound multi-threading ineffective - threads take turns rather than running in parallel. The GIL exists because Python's memory management isn't thread-safe. For I/O-bound tasks (network, files), threading still works because the GIL is released during I/O. For CPU-bound parallelism, use multiprocessing (separate processes, each with its own GIL) or alternative implementations like Jython or IronPython." },
            { text: "How does Python handle concurrency with the GIL?", ideal: "Python handles concurrency differently for I/O vs CPU-bound tasks. For I/O-bound work (web requests, file operations), threading works well because the GIL is released during I/O operations, allowing other threads to run. For CPU-bound work (calculations, data processing), threading doesn't help due to the GIL - only one thread executes at a time. Solutions: (1) Use multiprocessing to create separate processes, each with its own GIL and memory space, (2) Use async/await for I/O concurrency without threads, (3) Use C extensions that release the GIL, (4) Use PyPy or other Python implementations without a GIL." },
            { text: "What is the difference between an Interface and Abstract Class in Java?", ideal: "Interfaces define contracts (what methods must exist) without implementation (until Java 8's default methods). A class can implement multiple interfaces. Abstract classes can have both abstract methods (no implementation) and concrete methods (with implementation), plus instance variables and constructors. A class can only extend one abstract class. Interfaces use 'implements', abstract classes use 'extends'. Use interfaces for capabilities (Serializable, Comparable) that unrelated classes might share. Use abstract classes for shared code among related classes in a hierarchy." },
            { text: "When should you use an interface versus an abstract class?", ideal: "Use an interface when: (1) You want to define a contract that unrelated classes can implement (Runnable, Comparable), (2) You need multiple inheritance - a class can implement many interfaces, (3) You're defining capabilities or behaviors, not 'is-a' relationships. Use an abstract class when: (1) You have shared code that subclasses should inherit, (2) You need instance variables or constructors, (3) You have a clear class hierarchy (Animal -> Dog, Cat), (4) You want to provide default implementations that subclasses can override. Modern Java blurs this with default interface methods, but abstract classes still allow state (fields)." },
            { text: "Explain the Java Memory Model - Heap vs Stack.", ideal: "Stack memory stores method call frames, local primitive variables, and object references. Each thread has its own stack. Stack is LIFO - when a method returns, its frame is popped and memory is immediately freed. Heap memory stores all objects and instance variables. Heap is shared across all threads. Objects remain in heap until garbage collected. Example: void method() { int x = 5; String s = new String('hello'); } - x (primitive) and s (reference) go on stack, the String object goes on heap. Stack is faster but limited in size (can cause StackOverflowError). Heap is larger but slower and requires GC." },
            { text: "How does garbage collection work in Java?", ideal: "Java's garbage collector automatically frees memory by removing objects that are no longer reachable. It uses generational collection: Young Generation (Eden + Survivor spaces) for new objects, Old Generation for long-lived objects. Process: (1) Minor GC - cleans Young Gen frequently, moves survivors to Old Gen, (2) Major GC - cleans Old Gen less frequently, more expensive. Algorithms include Mark-and-Sweep (mark reachable objects, sweep unreachable ones) and G1GC (divides heap into regions, collects regions with most garbage first). You can't force GC but can suggest it with System.gc(). GC pauses can impact performance." },
            { text: "How does Node.js handle concurrency if single-threaded?", ideal: "Node.js achieves concurrency through non-blocking I/O and the event loop, not multi-threading. When you make an async operation (file read, network request), Node.js delegates it to the OS kernel or thread pool via libuv, then continues executing other code. When the operation completes, a callback is queued in the event loop. The single thread processes callbacks one at a time. This works well for I/O-bound tasks (web servers) because the thread isn't blocked waiting. For CPU-intensive tasks, use Worker Threads or child processes to avoid blocking the event loop." },
            { text: "Explain Node.js event-driven architecture.", ideal: "Node.js uses an event-driven, non-blocking architecture built on the event loop. The event loop continuously checks for pending events/callbacks. When you call async functions (fs.readFile, http.get), they're delegated to libuv's thread pool or OS, and a callback is registered. The main thread continues executing without waiting. When the operation completes, its callback enters the event loop queue. The loop processes callbacks in phases: timers (setTimeout), I/O callbacks, idle, poll (new I/O), check (setImmediate), close callbacks. This allows handling thousands of concurrent connections with a single thread, making Node.js ideal for I/O-heavy applications." },
            { text: "Explain ACID properties in databases.", ideal: "ACID ensures reliable database transactions: (1) Atomicity - transactions are all-or-nothing; if any part fails, the entire transaction rolls back. Example: transferring money must debit and credit, or neither. (2) Consistency - transactions move the database from one valid state to another, maintaining all rules and constraints. (3) Isolation - concurrent transactions don't interfere; each sees a consistent view as if running alone. Prevents dirty reads, phantom reads. (4) Durability - once committed, data persists even after crashes, using write-ahead logs. SQL databases prioritize ACID; NoSQL often trades it for performance/scalability." },
            { text: "What does ACID stand for in database systems?", ideal: "ACID stands for: Atomicity (transactions are indivisible - all operations succeed or all fail), Consistency (database remains in a valid state before and after transactions, enforcing constraints), Isolation (concurrent transactions don't see each other's intermediate states, preventing race conditions), Durability (committed transactions are permanent, surviving crashes through write-ahead logging). These properties guarantee data integrity in relational databases. Example: in a bank transfer, ACID ensures money isn't lost if the system crashes between debit and credit operations. NoSQL databases often relax ACID for better performance and scalability (BASE model instead)." },
            { text: "Difference between SQL and NoSQL databases?", ideal: "SQL databases are relational with fixed schemas, ACID transactions, and vertical scaling (PostgreSQL, MySQL). They use structured tables with relationships and SQL query language. NoSQL databases are non-relational with flexible schemas, eventual consistency, and horizontal scaling (MongoDB, Cassandra). They use documents, key-value pairs, or graphs. SQL excels at complex queries and data integrity. NoSQL excels at high-volume, unstructured data and rapid iteration. The choice depends on data structure, scale requirements, and consistency needs." },
            { text: "When should you use SQL versus NoSQL?", ideal: "Use SQL when you need: (1) Complex relationships and joins between entities, (2) ACID transactions for data integrity (banking, e-commerce), (3) Structured data with a stable schema, (4) Complex queries and reporting, (5) Strong consistency requirements. Use NoSQL when you need: (1) Massive scale and horizontal partitioning, (2) Flexible schema for rapidly changing data, (3) High write throughput, (4) Unstructured or semi-structured data (logs, social media), (5) Eventual consistency is acceptable. Many modern apps use both (polyglot persistence) - SQL for transactional data, NoSQL for caching or analytics." },
            { text: "What is Database Indexing and how does it work?", ideal: "Database indexing creates a data structure (usually B-Tree or Hash) that allows fast lookups without scanning every row. An index on a column stores sorted values with pointers to the actual rows. When you query WHERE email='user@example.com', the database uses the email index to find the row in O(log n) time instead of O(n) full table scan. Indexes work like a book's index - you look up the term, get the page number, then jump directly to that page. Composite indexes cover multiple columns. Indexes dramatically speed up SELECT queries but slow down INSERT/UPDATE/DELETE because the index must be maintained." },
            { text: "What are the tradeoffs of database indexing?", ideal: "Indexing tradeoffs: PROS: (1) Dramatically faster SELECT queries - O(log n) vs O(n), (2) Faster sorting and grouping, (3) Enforces uniqueness constraints, (4) Speeds up joins. CONS: (1) Slower writes - every INSERT/UPDATE/DELETE must update indexes, (2) Additional storage space - indexes can be as large as the table itself, (3) Index maintenance overhead during updates, (4) Too many indexes hurt performance. Best practice: Index columns used in WHERE, JOIN, ORDER BY clauses, but don't over-index. Monitor query performance and add indexes based on actual usage patterns, not speculation." },
            { text: "What is the CAP Theorem?", ideal: "The CAP Theorem states that a distributed system can only guarantee 2 of 3 properties simultaneously: (1) Consistency - all nodes see the same data at the same time, (2) Availability - every request receives a response (success or failure), (3) Partition Tolerance - system continues operating despite network failures between nodes. Since network partitions are inevitable in distributed systems, you must choose between CP (consistent but may be unavailable during partitions) or AP (available but may serve stale data). Examples: CP systems include traditional databases with strong consistency. AP systems include Cassandra, DynamoDB with eventual consistency." },
            { text: "Explain the tradeoffs in the CAP theorem.", ideal: "CAP theorem forces a choice during network partitions: CP (Consistency + Partition Tolerance) - System rejects writes during partitions to maintain consistency. All nodes show same data but some may be unavailable. Used in banking, inventory systems where correctness is critical. Examples: MongoDB, HBase, Redis. AP (Availability + Partition Tolerance) - System accepts writes during partitions, allowing temporary inconsistencies. All nodes respond but may show different data until reconciled. Used in social media, caching where availability matters more. Examples: Cassandra, DynamoDB, Riak. Modern systems often use tunable consistency, letting you choose per-operation. Note: CA (Consistency + Availability without partition tolerance) is impossible in distributed systems." },
            { text: "What is REST API design?", ideal: "REST (Representational State Transfer) is an architectural style for APIs using HTTP methods to perform operations on resources. Key principles: (1) Resource-based URLs (/users/123, not /getUser?id=123), (2) HTTP methods - GET (read), POST (create), PUT (update), DELETE (remove), (3) Stateless - each request contains all needed information, (4) Standard status codes (200 OK, 404 Not Found, 500 Error), (5) JSON/XML responses, (6) HATEOAS - responses include links to related resources. REST is simple, scalable, and cacheable, making it the most popular API style for web services." },
            { text: "What are REST API best practices?", ideal: "REST API best practices: (1) Use nouns for resources, not verbs (GET /users not GET /getUsers), (2) Use plural nouns (/users not /user), (3) Use HTTP methods correctly - GET is safe and idempotent, POST creates, PUT updates entirely, PATCH updates partially, (4) Version your API (/v1/users), (5) Use proper status codes - 200 success, 201 created, 400 bad request, 401 unauthorized, 404 not found, 500 server error, (6) Implement pagination for lists (?page=1&limit=20), (7) Support filtering and sorting (?status=active&sort=created_at), (8) Use HTTPS always, (9) Implement rate limiting, (10) Provide clear error messages with details, (11) Document with OpenAPI/Swagger." },
            { text: "Explain GraphQL versus REST.", ideal: "REST uses multiple endpoints (/users, /posts, /comments) with fixed response structures - you get all fields whether needed or not. GraphQL uses a single endpoint where clients specify exactly what data they need in a query. REST example: GET /users/1 returns all user fields. GraphQL: query { user(id: 1) { name email } } returns only name and email. GraphQL advantages: no over-fetching, no under-fetching (no multiple requests), strongly typed schema, single request for complex data. REST advantages: simpler, better caching, easier to understand, no query complexity issues. GraphQL is better for complex, nested data with varying client needs. REST is better for simple, stable APIs." },
            { text: "What are the advantages of GraphQL?", ideal: "GraphQL advantages: (1) No over-fetching - clients request only needed fields, reducing bandwidth, (2) No under-fetching - get related data in one request instead of multiple REST calls, (3) Strongly typed schema - self-documenting with introspection, (4) Single endpoint - simpler routing, (5) Flexible queries - frontend can evolve without backend changes, (6) Real-time subscriptions built-in, (7) Better developer experience with tools like GraphiQL. Disadvantages: (1) More complex than REST, (2) Harder to cache (POST requests), (3) Query complexity can cause performance issues, (4) Steeper learning curve, (5) Potential for expensive queries if not rate-limited. Best for: complex data requirements, mobile apps needing minimal data transfer, rapid frontend iteration." },
            { text: "What is database normalization?", ideal: "Database normalization is the process of organizing data to reduce redundancy and improve data integrity by dividing large tables into smaller, related tables. It eliminates duplicate data, ensures data dependencies make sense, and prevents update/insert/delete anomalies. For example, instead of storing customer address in every order, you store it once in a customers table and reference it. Normalization follows progressive normal forms (1NF, 2NF, 3NF, BCNF) with increasing strictness. Benefits: data consistency, easier updates, less storage. Tradeoff: more joins needed for queries. Most production databases aim for 3NF as a balance between normalization and performance." },
            { text: "Explain the different normal forms in databases.", ideal: "Normal forms are progressive rules for normalization: (1) 1NF (First Normal Form) - Each column contains atomic (indivisible) values, no repeating groups. Example: split 'phone1, phone2' into separate rows. (2) 2NF - Meets 1NF AND no partial dependencies - non-key columns depend on the entire primary key, not just part of it. Relevant for composite keys. (3) 3NF - Meets 2NF AND no transitive dependencies - non-key columns depend only on the primary key, not on other non-key columns. (4) BCNF (Boyce-Codd) - Stricter 3NF for certain edge cases. (5) 4NF, 5NF - Handle multi-valued dependencies, rarely used in practice. Most databases target 3NF for good balance." },
            { text: "What is database denormalization and when to use it?", ideal: "Denormalization adds redundancy for performance. Use for read-heavy systems where joins are expensive. Tradeoff: Faster reads, more storage, update complexity." },
            { text: "Explain database transactions and isolation levels.", ideal: "Transactions ensure ACID. Isolation levels: Read Uncommitted, Read Committed, Repeatable Read, Serializable. Higher isolation = more consistency, less concurrency." },
            { text: "What is the N+1 query problem?", ideal: "The N+1 query problem occurs when you fetch a list of N items (1 query), then make a separate query for each item's related data (N queries), totaling N+1 queries. Example: Fetch 100 users (1 query), then for each user fetch their posts (100 queries) = 101 total queries. This kills performance. The problem is common in ORMs with lazy loading. Instead of 101 queries, you should fetch all data in 1-2 queries using joins or eager loading. Example: SELECT users.*, posts.* FROM users LEFT JOIN posts ON users.id = posts.user_id. This is a classic performance anti-pattern in web applications." },
            { text: "How do you prevent the N+1 query problem?", ideal: "Prevent N+1 queries with: (1) Eager loading - load related data upfront. In Rails: User.includes(:posts), in Django: User.objects.prefetch_related('posts'), in Sequelize: User.findAll({ include: [Post] }). (2) Use JOIN queries to fetch everything at once. (3) DataLoader pattern (GraphQL) - batches and caches requests. (4) Query in bulk - fetch all related IDs, then query once: post_ids = users.map(&:post_id); Post.where(id: post_ids). (5) Use database query logging to detect N+1 patterns. (6) Tools like Bullet gem (Rails) warn about N+1 queries in development. Always profile your queries and look for repeated patterns." },
            { text: "What is connection pooling?", ideal: "Connection pooling reuses database connections instead of creating new ones per request. Improves performance by reducing connection overhead. Configure pool size based on load." },
            { text: "Explain microservices architecture.", ideal: "Microservices architecture splits an application into small, independent services that communicate via APIs (usually HTTP/REST or message queues). Each service owns its own database, has a single responsibility, and can be deployed independently. Example: E-commerce might have separate services for users, products, orders, payments, notifications. Each service can use different technologies, scale independently, and be developed by different teams. Services communicate asynchronously when possible. This contrasts with monolithic architecture where everything is one large application. Microservices enable faster development, better scalability, and fault isolation, but add complexity in deployment, monitoring, and data consistency." },
            { text: "What are the pros and cons of microservices?", ideal: "Microservices PROS: (1) Independent scaling - scale only the services that need it, (2) Technology diversity - use best tool for each service, (3) Faster deployment - deploy services independently without affecting others, (4) Fault isolation - one service failure doesn't crash entire system, (5) Team autonomy - teams own services end-to-end, (6) Easier to understand - each service is smaller. CONS: (1) Distributed system complexity - network latency, failures, (2) Data consistency challenges - no ACID across services, (3) More operational overhead - deploy, monitor, log many services, (4) Testing complexity - integration testing is harder, (5) Network overhead - inter-service communication, (6) Debugging difficulty - trace requests across services. Best for: large teams, complex domains, high scale requirements." },
            { text: "What is message queue and when to use it?", ideal: "Message queues (RabbitMQ, Kafka) decouple services via async communication. Use for: background jobs, event-driven systems, load leveling, system decoupling." },
            { text: "Explain pub/sub messaging pattern.", ideal: "Pub/Sub decouples publishers and subscribers. Publishers send messages to topics, subscribers receive from topics they're interested in. Enables event-driven architecture." },
            { text: "What is API rate limiting?", ideal: "Rate limiting restricts number of requests per time period. Prevents abuse, ensures fair usage, protects system resources. Implement using Redis counters or token bucket algorithm." },
            { text: "How do you implement authentication in APIs?", ideal: "Common methods: JWT (stateless tokens), OAuth (third-party), API Keys (simple), Session Cookies (stateful). Choose based on use case and security requirements." },
            { text: "What is database sharding?", ideal: "Sharding splits data across multiple databases. Horizontal sharding distributes rows, vertical splits columns. Improves scalability but adds query complexity." },
            { text: "Explain horizontal vs vertical scaling.", ideal: "Horizontal scaling adds more servers (scale out). Vertical scaling adds more resources to one server (scale up). Horizontal is more scalable, vertical has limits." },
            { text: "What is caching and different caching strategies?", ideal: "Caching stores frequently accessed data in fast storage. Strategies: Cache-aside (lazy), Write-through (sync), Write-back (async). Use Redis or Memcached." },
            { text: "What is webhook versus polling?", ideal: "Polling repeatedly checks for updates (resource intensive). Webhooks push notifications when events occur (efficient). Use webhooks for real-time updates." },
            { text: "Explain SQL injection and prevention.", ideal: "SQL injection injects malicious SQL code through user input. Prevent using: parameterized queries, prepared statements, input validation, ORMs, least privilege." },
            { text: "What is database replication?", ideal: "Replication copies data across servers. Master-slave: one writes, many read. Master-master: all can write. Improves availability and read performance." },
            { text: "Explain eventual consistency.", ideal: "Eventual consistency guarantees data becomes consistent over time, not immediately. Used in distributed systems prioritizing availability over instant consistency." },
            { text: "What is load balancing?", ideal: "Load balancers distribute traffic across servers. Algorithms: Round-robin, Least connections, IP hash. Improves availability, scalability, and performance." },
            { text: "Explain containerization with Docker.", ideal: "Docker packages applications with dependencies into containers. Benefits: consistency across environments, isolation, portability, efficient resource usage." },
            { text: "What is the difference between Docker and VMs?", ideal: "VMs virtualize hardware with full OS. Containers share host OS kernel, virtualizing only the application layer. Containers are lighter and faster." },
            { text: "What is CI/CD pipeline?", ideal: "CI/CD automates code integration and deployment. CI runs tests on commits. CD deploys to production. Tools: Jenkins, GitLab CI, GitHub Actions." },
            { text: "Explain the purpose of monitoring and logging.", ideal: "Monitoring tracks system health and performance metrics. Logging records events for debugging and auditing. Together they enable observability and incident response." },
            { text: "What is the difference between synchronous and asynchronous processing?", ideal: "Synchronous blocks until operation completes. Asynchronous continues execution, handling result later via callbacks/promises. Async improves responsiveness." }
        ]
    },
    {
        id: 'sde-fullstack',
        title: 'Full Stack Development',
        description: 'Frontend, Backend, APIs, and System Design.',
        icon: 'Database',
        questions: [
            // === FRONTEND FUNDAMENTALS ===
            { text: "What is the Virtual DOM in React and why is it important?", ideal: "The Virtual DOM is a lightweight copy of the real DOM kept in memory. When state changes, React creates a new Virtual DOM, diffs it with the previous one, and only updates the changed parts in the real DOM. This makes rendering much faster." },
            { text: "Explain the difference between props and state in React.", ideal: "Props are read-only data passed from parent to child components. State is mutable data managed within a component. Props flow down, state is local. When state changes, the component re-renders." },
            { text: "What is the difference between let, const, and var in JavaScript?", ideal: "var is function-scoped and hoisted. let is block-scoped and not hoisted. const is block-scoped and cannot be reassigned. Use const by default, let when you need to reassign, avoid var." },
            { text: "What are closures in JavaScript?", ideal: "A closure is a function that has access to variables from its outer scope even after the outer function has returned. Closures are used for data privacy, callbacks, and maintaining state in functional programming." },
            { text: "What is the event loop in JavaScript?", ideal: "The event loop is JavaScript's concurrency model. It continuously checks the call stack and the callback queue. When the stack is empty, it pushes callbacks from the queue to the stack. This enables async behavior in single-threaded JS." },
            { text: "Explain CSS Flexbox and when you would use it.", ideal: "Flexbox is a one-dimensional layout method for arranging items in rows or columns. Key properties: display:flex, justify-content (main axis), align-items (cross axis). Use it for navigation bars, card layouts, centering elements." },
            { text: "What is responsive web design?", ideal: "Responsive design makes web pages look good on all devices. Techniques: media queries, flexible grids, responsive images, viewport meta tag. Mobile-first approach: design for mobile, then enhance for larger screens." },
            { text: "What are React hooks? Name 3 commonly used hooks.", ideal: "Hooks let you use state and lifecycle features in functional components. Common hooks: useState (state management), useEffect (side effects like API calls), useContext (accessing context). Also: useRef, useMemo, useCallback." },
            // === BACKEND FUNDAMENTALS ===
            { text: "What is a RESTful API? Explain its key principles.", ideal: "REST uses HTTP methods (GET, POST, PUT, DELETE) for CRUD operations. Key principles: stateless, client-server architecture, uniform interface, resource-based URLs. Example: GET /users returns all users, POST /users creates one." },
            { text: "What is the difference between SQL and NoSQL databases?", ideal: "SQL databases are relational, use tables with schemas, support ACID transactions (PostgreSQL, MySQL). NoSQL databases are non-relational, schema-flexible, better for unstructured data (MongoDB, Redis). Choose SQL for complex queries, NoSQL for scalability." },
            { text: "Explain middleware in Express.js or any web framework.", ideal: "Middleware are functions that execute between receiving a request and sending a response. They can modify req/res, end the request cycle, or call next(). Used for: authentication, logging, error handling, CORS, body parsing." },
            { text: "What is an ORM and why would you use one?", ideal: "ORM (Object-Relational Mapping) maps database tables to objects in code. Examples: Sequelize, Prisma, SQLAlchemy. Benefits: write database queries in your language instead of SQL, easier migrations, prevents SQL injection. Drawback: abstraction can be slow for complex queries." },
            { text: "What are environment variables and why are they important?", ideal: "Environment variables store configuration outside code — API keys, database URLs, secrets. They keep sensitive data out of source code, allow different configs per environment (dev/staging/prod). Access via process.env in Node.js." },
            { text: "Explain the difference between authentication and authorization.", ideal: "Authentication verifies WHO you are (login with username/password, JWT token). Authorization determines WHAT you can access (admin vs regular user). Auth first, then authorization. Example: JWT for auth, role-based access for authorization." },
            // === FULLSTACK / API ===
            { text: "What is CORS and how do you fix CORS errors?", ideal: "CORS (Cross-Origin Resource Sharing) prevents sites from calling APIs on different domains. Fix: Server sends header 'Access-Control-Allow-Origin: *' or specific domain. In Express: use cors() middleware." },
            { text: "How would you handle authentication — JWT vs Sessions?", ideal: "Sessions: Server stores state, returns Cookie ID. Good for revocation. JWT: Stateless, data in signed token. Scalable but harder to revoke. Use JWT for microservices, sessions for traditional web apps." },
            { text: "What is the difference between HTTP and HTTPS?", ideal: "HTTP sends data in plain text, HTTPS encrypts it using TLS/SSL. HTTPS prevents man-in-the-middle attacks, data tampering, and eavesdropping. Modern web requires HTTPS for security and SEO." },
            { text: "What are WebSockets and when would you use them?", ideal: "WebSockets provide full-duplex communication between client and server over a single connection. Unlike HTTP, the server can push data to the client. Use for: real-time chat, live notifications, stock tickers, multiplayer games." },
            { text: "What is Git branching strategy? Explain feature branches.", ideal: "Git branches isolate work. Main/master is production. Feature branches are created for new features, merged via pull requests. Common strategies: Git Flow (develop/release branches), trunk-based development (short-lived branches)." },
            { text: "How would you deploy a full-stack application?", ideal: "Frontend: Build static files, deploy to CDN (Vercel, Netlify). Backend: Containerize with Docker, deploy to cloud (AWS, Heroku). Database: Managed service (RDS, MongoDB Atlas). CI/CD pipeline for automated testing and deployment." },
            // === SYSTEM DESIGN (keep some for variety) ===
            { text: "Design a ride-sharing service like Uber.", ideal: "1. Matching: Geo-hash for nearby drivers. 2. DB: Cassandra for trips. 3. Real-time: WebSocket for location. 4. Payment: Stripe API. 5. Pricing: Surge algorithm." },
            { text: "How do you implement geolocation matching?", ideal: "1. Matching: Geo-hash for nearby drivers. 2. DB: Cassandra for trips. 3. Real-time: WebSocket for location. 4. Payment: Stripe API. 5. Pricing: Surge algorithm." },
            { text: "Design a parking lot system.", ideal: "OOP Design: Classes for ParkingLot, Level, Spot, Vehicle. Methods: park(vehicle), unpark(ticket). Use Strategy pattern for pricing. HashMap for spot lookup." },
            { text: "Design an elevator system.", ideal: "Algorithm: SCAN (elevator algorithm). Variables: current floor, direction, queue. Optimize: Direction-first queue, minimize total wait time. Handle edge cases: fire mode." },
            { text: "Explain database sharding strategies.", ideal: "Hash-based: hash(user_id) % num_shards. Range-based: user_id ranges per shard. Geography-based: shard by region. Trade-off: Hash for balance, Range for range queries." },
            { text: "Design a job scheduler.", ideal: "1. Queue: Priority queue for jobs. 2. Workers: Pool of workers polling queue. 3. Scheduling: Cron expressions. 4. Retry: Exponential backoff. 5. Monitor: Dead letter queue." },
            { text: "What is eventual consistency in distributed systems?", ideal: "Data eventually becomes consistent across nodes, not immediately. Used when availability is prioritized. Example: Amazon cart - see stale data briefly but system stays available." },
            { text: "Design a global counter service.", ideal: "Centralized: Single Redis (simple, bottleneck). Distributed: Shard counters, aggregate periodically (scalable, eventual consistency). Trade-off based on accuracy requirements." },
            { text: "Explain the two-phase commit protocol.", ideal: "Coordinator asks participants to prepare (phase 1). If all agree, coordinator commits (phase 2). Ensures atomic transactions across distributed systems but blocks on failure." },
            { text: "Design a logging and monitoring system.", ideal: "1. Collection: Agents on servers send logs. 2. Aggregation: Kafka streams. 3. Processing: ELK stack (Elasticsearch, Logstash, Kibana). 4. Alerts: Thresholds trigger notifications." },
            { text: "What is the circuit breaker pattern?", ideal: "Circuit breaker prevents cascading failures. States: Closed (normal), Open (failing, reject fast), Half-Open (testing). Protects against overloading failing services." },
            { text: "Design a distributed lock.", ideal: "Implementation: Redis SETNX with expiration. Algorithm: Redlock for multi-master. Use for: ensuring single execution of critical sections in distributed systems." },
            { text: "Explain the saga pattern for distributed transactions.", ideal: "Saga breaks transaction into steps with compensating actions. If step fails, execute compensations to rollback. Types: Choreography (events), Orchestration (coordinator)." },
            { text: "Design a content delivery network (CDN).", ideal: "1. Edge servers close to users. 2. Origin pull or push. 3. Cache: popular content. 4. Routing: DNS/Anycast to nearest server. 5. Invalidation: cache expiration policies." },
            { text: "What is database partitioning?", ideal: "Partitioning splits a table into smaller pieces. Horizontal: split rows (sharding). Vertical: split columns. Improves performance and manageability of large datasets." },
            { text: "Design a real-time analytics system.", ideal: "1. Ingestion: Kafka for stream processing. 2. Processing: Apache Flink/Spark Streaming. 3. Storage: Time-series DB (InfluxDB). 4. Visualization: Grafana dashboards." },
            { text: "Explain the bulkhead pattern.", ideal: "Bulkhead isolates resources for different services. Example: separate connection pools per service. Prevents one failing service from consuming all resources." },
            { text: "Design an API gateway.", ideal: "API Gateway: Single entry routing requests to microservices. Features: authentication, rate limiting, request aggregation, protocol translation, logging." },
            { text: "What is service discovery in microservices?", ideal: "Services register with discovery server (Consul, Eureka). Clients query to find service instances. Patterns: Client-side discovery, Server-side discovery (load balancer)." },
            { text: "Design a distributed key-value store.", ideal: "1. Consistent hashing for distribution. 2. Replication for availability. 3. Vector clocks for versioning. 4. Gossip protocol for membership. 5. Merkle trees for anti-entropy." },
            { text: "Explain the CQRS pattern.", ideal: "CQRS (Command Query Responsibility Segregation) separates read and write models. Benefits: optimized queries, independent scaling. Trade-off: eventual consistency complexity." },
            { text: "Design a distributed transaction system.", ideal: "Options: 2PC (strong consistency, blocking), Saga (eventual consistency, compensations), Event Sourcing (audit trail). Choose based on consistency requirements." },
            { text: "What is backpressure in stream processing?", ideal: "Backpressure handles slow consumers by signaling producers to slow down. Strategies: buffering, dropping, blocking. Prevents system overload from fast data streams." },
            { text: "Design a deployment pipeline.", ideal: "Stages: Code -> Build -> Test -> Stage -> Production. Gates: automated tests, manual approval. Strategy: Blue-green or canary deployment. Rollback: previous version ready." },
            { text: "Explain idempotency in distributed systems.", ideal: "Idempotent operations produce same result when called multiple times. Critical for retry logic and exactly-once semantics. Implement using unique request IDs." },
            { text: "Design a multi-tenant system.", ideal: "Isolation: Separate DB per tenant or shared DB with tenant_id. Features: custom domains, white-labeling. Security: row-level security, tenant context. Pricing: usage-based billing." },
            { text: "What is the strangler fig pattern?", ideal: "Incrementally replace legacy system by routing new features to new system while old features use old system. Gradually migrate until old system is completely replaced." }
        ]
    },
    {
        id: 'behavioral',
        title: 'Behavioral / HR',
        description: 'Leadership, Conflict Resolution, and Culture Fit.',
        icon: 'Users',
        questions: [
            { text: "Tell me about a time you had a conflict with a coworker.", ideal: "STAR Method: Situation (API design disagreement), Task (Need to ship), Action (1:1 discussion, listened, found middle ground), Result (Shipped on time, improved relationship)." },
            { text: "How do you handle disagreements in a team?", ideal: "STAR Method: Situation (API design disagreement), Task (Need to ship), Action (1:1 discussion, listened, found middle ground), Result (Shipped on time, improved relationship)." },
            { text: "Describe a situation where you met a tight deadline.", ideal: "Situation: Feature due in 2 days. Action: Prioritized MVP, cut non-essentials, communicated early. Result: Delivered core product, added features later." },
            { text: "How do you prioritize when everything is urgent?", ideal: "Situation: Feature due in 2 days. Action: Prioritized MVP, cut non-essentials, communicated early. Result: Delivered core product, added features later." },
            { text: "Tell me about a time you failed. What did you learn?", ideal: "Be honest: 'Deployed code that crashed prod without checking logs. Learned importance of automated testing and staging. Haven't repeated that mistake.'" },
            { text: "Describe a mistake you made and how you handled it.", ideal: "Be honest: 'Deployed code that crashed prod without checking logs. Learned importance of automated testing and staging. Haven't repeated that mistake.'" },
            { text: "Tell me about a time you showed leadership.", ideal: "Situation: Team lacked direction. Action: Organized tech design doc reviews, mentored juniors, proposed standards. Result: Improved code quality, team velocity." },
            { text: "How do you mentor junior developers?", ideal: "Situation: Team lacked direction. Action: Organized tech design doc reviews, mentored juniors, proposed standards. Result: Improved code quality, team velocity." },
            { text: "Describe a time you had to learn something new quickly.", ideal: "Situation: New tech stack for project. Action: Online courses, documentation, pair programming. Result: Became productive within 2 weeks, taught others." },
            { text: "How do you stay updated with technology?", ideal: "Situation: New tech stack for project. Action: Online courses, documentation, pair programming. Result: Became productive within 2 weeks, taught others." },
            { text: "Tell me about a time you improved a process.", ideal: "Situation: Slow deployments. Action: Implemented CI/CD pipeline, automated tests. Result: Reduced deployment time from hours to minutes, fewer bugs." },
            { text: "How do you handle constructive criticism?", ideal: "Listen actively, ask clarifying questions, reflect on feedback, implement changes, follow up. View as growth opportunity, not personal attack." },
            { text: "Describe your ideal team culture.", ideal: "Collaborative, open communication, psychological safety, continuous learning, work-life balance, celebrating wins, learning from failures, diverse perspectives." },
            { text: "How do you handle working with difficult personalities?", ideal: "Professional communication, focus on work goals, find common ground, involve manager if needed, maintain boundaries, empathy." },
            { text: "Tell me about a time you went above and beyond.", ideal: "Situation: Critical bug before launch. Action: Worked extra hours, root cause analysis, fixed and documented. Result: Successful launch, prevented future issues." },
            { text: "What motivates you in your work?", ideal: "Solving complex problems, learning new technologies, team collaboration, user impact, building quality products, continuous improvement." },
            { text: "How do you handle stress and pressure?", ideal: "Prioritize tasks, break down problems, take breaks, communicate with team, maintain work-life balance, exercise, time management." },
            { text: "Describe a time you had to make a difficult decision.", ideal: "Situation: Choose between features and technical debt. Action: Analyzed impact, discussed with team, decided priorities. Result: Made informed trade-off." },
            { text: "How do you ensure code quality?", ideal: "Code reviews, automated testing, pair programming, linting, documentation, design patterns, refactoring, continuous integration." },
            { text: "Tell me about a time you received negative feedback.", ideal: "Listened without defending, asked for specifics, thanked them, reflected, made changes, followed up. Showed growth mindset." },
            { text: "What are your career goals?", ideal: "Growth into senior/lead roles, deep technical expertise, mentoring, architectural decisions, staying current with technology, work-life balance." },
            { text: "How do you approach problem-solving?", ideal: "Understand problem, break into components, research solutions, prototype, test, iterate, document, review with team." },
            { text: "Describe your communication style.", ideal: "Clear, concise, context-aware, active listening, adjust by audience, documentation, visual aids, open to feedback." },
            { text: "How do you handle multiple projects?", ideal: "Prioritization matrix, time blocking, clear communication, realistic estimates, saying no when needed, project management tools." },
            { text: "Tell me why you want to work here.", ideal: "Research company: mission alignment, technology stack interest, team culture, growth opportunities, product impact. Be specific and genuine." },
            { text: "What is your greatest strength?", ideal: "Pick relevant strength: problem solving, communication, learning ability. Provide specific example demonstrating it in action." },
            { text: "What is your greatest weakness?", ideal: "Real weakness with growth plan: 'Impatient with slow processes. Learning to balance speed with thoroughness through code reviews.'" },
            { text: "How do you handle ambiguity?", ideal: "Ask clarifying questions, make reasonable assumptions, document decisions, iterate based on feedback, stay flexible." },
            { text: "Describe a time you influenced without authority.", ideal: "Situation: Proposed new tool. Action: Created demo, showed benefits, addressed concerns, got buy-in. Result: Team adopted it." },
            { text: "How do you balance technical debt with new features?", ideal: "Assess impact and risk, allocate percentage of sprints to debt, make visible in planning, advocate for user impact." },
            { text: "What questions do you have for me?", ideal: "Ask about: team structure, tech stack, challenges, growth opportunities, code review process, deployment frequency, learning culture." },
            { text: "Tell me about yourself.", ideal: "Brief professional summary: background, key experiences, relevant skills, why interested in role. Keep concise (2-3 minutes)." },
            { text: "Why are you leaving your current job?", ideal: "Focus on growth opportunities, new challenges, company mission. Stay positive about current employer. Avoid negativity." },
            { text: "How do you handle work-life balance?", ideal: "Set boundaries, time management, prioritize health, hobbies outside work, communicate needs, sustainable pace." },
            { text: "Describe a time you collaborated across teams.", ideal: "Situation: Feature needed backend and frontend. Action: Regular syncs, shared docs, aligned on API contract. Result: Smooth integration." },
            { text: "How do you give feedback to peers?", ideal: "Timely, specific, actionable, private for criticism, public for praise, focus on behavior not person, balanced, follow up." },
            { text: "What makes you a good fit for this role?", ideal: "Match skills to job requirements, cite specific experiences, show enthusiasm, explain how you'd add value." },
            { text: "How do you handle changing requirements?", ideal: "Stay flexible, understand reason for change, assess impact, communicate with stakeholders, adjust plans, document changes." },
            { text: "Tell me about a time you took initiative.", ideal: "Situation: Saw inefficient process. Action: Researched solutions, proposed improvement, implemented. Result: Time savings, better workflow." },
            { text: "How do you measure success?", ideal: "User impact, code quality, team collaboration, meeting deadlines, learning growth, business value delivered." },
            { text: "Describe your debugging process.", ideal: "Reproduce issue, read error logs, rubber duck debugging, binary search, use debugger, add logging, test hypothesis, root cause analysis." },
            { text: "How do you handle production incidents?", ideal: "Stay calm, assess severity, communicate status, fix immediately or rollback, root cause analysis, post-mortem, prevent recurrence." },
            { text: "What do you do when you don't know the answer?", ideal: "Admit it honestly, explain approach to find answer, research, ask experts, document learning, share with team." },
            { text: "How do you ensure you're building the right thing?", ideal: "User research, gather requirements, prototype, get feedback, iterate, metrics, communicate with stakeholders." },
            { text: "Tell me about a time you had to convince someone.", ideal: "Situation: Advocated for testing. Action: Showed data on bugs caught, cost of production issues. Result: Team adopted TDD." },
            { text: "How would your colleagues describe you?", ideal: "Collaborative, reliable, problem-solver, good communicator, continuous learner. Support with examples." },
            { text: "What's the most impactful project you've worked on?", ideal: "Describe project: problem solved, your role, technologies, challenges overcame, measurable impact, what you learned." },
            { text: "How do you keep your team motivated?", ideal: "Recognize achievements, clear goals, remove blockers, provide growth opportunities, inclusive culture, celebrate wins." },
            { text: "Describe your approach to code reviews.", ideal: "Constructive feedback, explain why, suggest alternatives, appreciate good code, nitpick separately, security and performance checks." },
            { text: "What technology are you most excited about?", ideal: "Pick relevant technology, explain why, how it solves problems, what you're learning, how it applies to role." }
        ]
    },
    {
        id: 'data-science',
        title: 'Data Science & ML',
        description: 'Statistics, ML Models, and Python.',
        icon: 'Brain',
        questions: [
            { text: "Explain the bias-variance tradeoff.", ideal: "Bias is error from assumptions (underfitting). Variance is sensitivity to fluctuations (overfitting). Balance them to minimize total error." },
            { text: "How do you balance bias and variance in models?", ideal: "Bias is error from assumptions (underfitting). Variance is sensitivity to fluctuations (overfitting). Balance them to minimize total error." },
            { text: "What is Overfitting and how do you prevent it?", ideal: "Overfitting learns noise instead of signal (high train accuracy, low test accuracy). Prevent: Cross-validation, Regularization (L1/L2), more data, dropout." },
            { text: "How do you detect overfitting in your model?", ideal: "Overfitting learns noise instead of signal (high train accuracy, low test accuracy). Prevent: Cross-validation, Regularization (L1/L2), more data, dropout." },
            { text: "Explain supervised versus unsupervised learning.", ideal: "Supervised: Training with labeled data (input -> output). Examples: Classification, Regression. Unsupervised: Unlabeled data (find patterns). Examples: Clustering, PCA." },
            { text: "What is the difference between classification and regression?", ideal: "Supervised: Training with labeled data (input -> output). Examples: Classification, Regression. Unsupervised: Unlabeled data (find patterns). Examples: Clustering, PCA." },
            { text: "Explain cross-validation and its importance.", ideal: "Cross-validation splits data into k folds, trains on k-1, validates on 1, rotates. Provides robust performance estimate, prevents overfitting to single test set." },
            { text: "What is k-fold cross-validation?", ideal: "Cross-validation splits data into k folds, trains on k-1, validates on 1, rotates. Provides robust performance estimate, prevents overfitting to single test set." },
            { text: "Explain precision versus recall.", ideal: "Precision: Of predicted positives, how many are correct (TP/(TP+FP)). Recall: Of actual positives, how many found (TP/(TP+FN)). F1-score balances both." },
            { text: "When would you optimize for precision vs recall?", ideal: "Precision: Of predicted positives, how many are correct (TP/(TP+FP)). Recall: Of actual positives, how many found (TP/(TP+FN)). F1-score balances both." },
            { text: "What is regularization?", ideal: "Regularization adds penalty for complexity to loss function. L1 (Lasso) zeroes coefficients, L2 (Ridge) shrinks them. Prevents overfitting." },
            { text: "Explain L1 vs L2 regularization.", ideal: "Regularization adds penalty for complexity to loss function. L1 (Lasso) zeroes coefficients, L2 (Ridge) shrinks them. Prevents overfitting." },
            { text: "What is gradient descent?", ideal: "Optimization algorithm that iteratively moves toward minimum of loss function by following negative gradient. Variants: Batch, Stochastic, Mini-batch." },
            { text: "Explain different types of gradient descent.", ideal: "Optimization algorithm that iteratively moves toward minimum of loss function by following negative gradient. Variants: Batch, Stochastic, Mini-batch." },
            { text: "What is a confusion matrix?", ideal: "Table showing True Positives, False Positives, True Negatives, False Negatives. Used to calculate precision, recall, accuracy, F1-score." },
            { text: "How do you evaluate a classification model?", ideal: "Table showing True Positives, False Positives, True Negatives, False Negatives. Used to calculate precision, recall, accuracy, F1-score." },
            { text: "Explain decision trees and random forests.", ideal: "Decision trees split data on features. Random forests ensemble of trees on random subsets. Reduces variance, handles non-linearity, feature importance." },
            { text: "What are the advantages of random forests?", ideal: "Decision trees split data on features. Random forests ensemble of trees on random subsets. Reduces variance, handles non-linearity, feature importance." },
            { text: "What is feature engineering?", ideal: "Creating new features from raw data. Techniques: encoding categorical, scaling numerical, polynomial features, domain knowledge transformations." },
            { text: "How do you handle categorical variables?", ideal: "Creating new features from raw data. Techniques: encoding categorical, scaling numerical, polynomial features, domain knowledge transformations." },
            { text: "Explain Principal Component Analysis (PCA).", ideal: "PCA reduces dimensionality by finding orthogonal axes of maximum variance. Uses: visualization, noise reduction, speed up algorithms." },
            { text: "When would you use dimensionality reduction?", ideal: "PCA reduces dimensionality by finding orthogonal axes of maximum variance. Uses: visualization, noise reduction, speed up algorithms." },
            { text: "What is the difference between bagging and boosting?", ideal: "Bagging: Parallel ensemble (Random Forest). Boosting: Sequential, focuses on misclassified (XGBoost, AdaBoost). Boosting often higher accuracy, bagging reduces variance." },
            { text: "Explain ensemble methods.", ideal: "Bagging: Parallel ensemble (Random Forest). Boosting: Sequential, focuses on misclassified (XGBoost, AdaBoost). Boosting often higher accuracy, bagging reduces variance." },
            { text: "What is A/B testing?", ideal: "Randomly assigning users to variants (A or B) to measure impact. Statistical test determines significance. Used for feature experiments." },
            { text: "How do you determine statistical significance?", ideal: "Randomly assigning users to variants (A or B) to measure impact. Statistical test determines significance. Used for feature experiments." },
            { text: "Explain the central limit theorem.", ideal: "Sample means of large samples approximate normal distribution regardless of population distribution. Foundation for statistical inference." },
            { text: "What is a p-value?", ideal: "Probability of observing results at least as extreme as observed, assuming null hypothesis is true. p < 0.05 typically indicates statistical significance." },
            { text: "Explain type I and type II errors.", ideal: "Type I (false positive): Reject true null hypothesis. Type II (false negative): Fail to reject false null hypothesis. Trade-off controlled by significance level." },
            { text: "What is feature selection?", ideal: "Selecting relevant features to improve model. Methods: Filter (correlation), Wrapper (forward/backward), Embedded (Lasso). Benefits: reduce overfitting, faster training." },
            { text: "How do you handle imbalanced datasets?", ideal: "Techniques: Resampling (SMOTE, undersampling), class weights, anomaly detection, different metrics (F1 instead of accuracy), ensemble methods." },
            { text: "What is the ROC curve?", ideal: "Plot of True Positive Rate vs False Positive Rate across thresholds. AUC (Area Under Curve) measures overall performance. Higher AUC = better model." },
            { text: "Explain neural networks basics.", ideal: "Network of interconnected neurons (layers). Forward pass computes output. Backpropagation updates weights via gradient descent. Activation functions add non-linearity." },
            { text: "What is backpropagation?", ideal: "Network of interconnected neurons (layers). Forward pass computes output. Backpropagation updates weights via gradient descent. Activation functions add non-linearity." },
            { text: "What is the vanishing gradient problem?", ideal: "Gradients become very small in deep networks, preventing learning in early layers. Solutions: ReLU activation, batch normalization, residual connections." },
            { text: "Explain different activation functions.", ideal: "Sigmoid (0-1, vanishing gradient), Tanh (-1 to 1), ReLU (max(0,x), most common), Leaky ReLU (prevents dead neurons), Softmax (output layer probabilities)." },
            { text: "What is dropout in neural networks?", ideal: "Randomly disables neurons during training to prevent overfitting. Forces network to learn robust features. Turned off during inference." },
            { text: "Explain batch normalization.", ideal: "Normalizes layer inputs to stabilize learning. Benefits: faster training, higher learning rates, regularization effect, reduces gradient issues." },
            { text: "What is transfer learning?", ideal: "Using pre-trained model as starting point. Fine-tune on specific task. Benefits: Less data needed, faster training, better performance on small datasets." },
            { text: "Explain convolutional neural networks (CNNs).", ideal: "CNNs use convolution layers for spatial features. Applications: image classification, object detection. Components: Conv layers, pooling, fully connected." },
            { text: "What are recurrent neural networks (RNNs)?", ideal: "RNNs process sequential data with loops preserving state. Variants: LSTM (long-term memory), GRU (simpler). Applications: NLP, time series." },
            { text: "Explain the attention mechanism.", ideal: "Attention weighs input importance dynamically. Used in Transformers. Allows models to focus on relevant parts. Key innovation for modern NLP." },
            { text: "What is the transformer architecture?", ideal: "Attention-based architecture without recurrence. Components: Self-attention, positional encoding, feed-forward. Powers BERT, GPT, etc." },
            { text: "How do you handle missing data?", ideal: "Methods: Remove rows/columns, imputation (mean, median, mode, KNN), predict missing values, flag missingness as feature. Choose based on amount and pattern." },
            { text: "What is data normalization vs standardization?", ideal: "Normalization scales to [0,1] range. Standardization centers at 0 with std=1. Use standardization for normal distributions, normalization for bounded ranges." },
            { text: "Explain hyperparameter tuning.", ideal: "Finding optimal model parameters. Methods: Grid search, random search, Bayesian optimization. Use cross-validation to prevent overfitting." },
            { text: "What is the curse of dimensionality?", ideal: "As dimensions increase, data becomes sparse. Distance metrics lose meaning. Models need exponentially more data. Solution: dimensionality reduction." },
            { text: "Explain recommendation systems.", ideal: "Collaborative filtering (user/item similarity), content-based (item features), hybrid. Challenges: cold start, scalability, evaluation." },
            { text: "What is time series forecasting?", ideal: "Predicting future values from historical data. Methods: ARIMA, exponential smoothing, LSTM. Components: trend, seasonality, residuals." },
            { text: "How do you detect anomalies?", ideal: "Statistical methods (z-score), isolation forest, autoencoders, one-class SVM. Use for fraud detection, system monitoring." },
            { text: "What is reinforcement learning?", ideal: "Agent learns by interacting with environment. Receives rewards/penalties. Goal: maximize cumulative reward. Applications: games, robotics, optimization." },
            { text: "Explain model deployment considerations.", ideal: "Versioning, monitoring, retraining strategy, latency requirements, scalability, A/B testing, rollback plan, data drift detection." }
        ]
    },
    {
        id: 'java-basic',
        title: 'Java Programming (Basic)',
        description: 'Java Fundamentals, OOP Basics, and Core Syntax.',
        icon: 'Code',
        questions: [
            { text: "What is the difference between JDK, JRE, and JVM?", ideal: "JDK (Java Development Kit) contains tools for developing Java applications including compiler (javac), debugger, and JRE. JRE (Java Runtime Environment) contains libraries and JVM needed to run Java applications. JVM (Java Virtual Machine) executes Java bytecode and provides platform independence. In short: JDK = JRE + Development Tools, JRE = JVM + Libraries, JVM = Execution Engine." },
            { text: "Explain the main method signature in Java.", ideal: "public static void main(String[] args) is the entry point of Java programs. 'public' makes it accessible from anywhere, 'static' allows it to be called without creating an object, 'void' means it returns nothing, 'main' is the method name, and 'String[] args' accepts command-line arguments as an array of strings." },
            { text: "What are the primitive data types in Java?", ideal: "Java has 8 primitive types: byte (8-bit), short (16-bit), int (32-bit), long (64-bit) for integers; float (32-bit) and double (64-bit) for decimals; char (16-bit Unicode character); and boolean (true/false). Primitives are stored directly in memory, not as objects." },
            { text: "What is the difference between int and Integer?", ideal: "int is a primitive type that stores numeric values directly. Integer is a wrapper class that wraps int in an object, allowing it to be used in collections and providing utility methods like parseInt(). Autoboxing automatically converts int to Integer and vice versa. Use int for performance, Integer when you need an object." },
            { text: "Explain String immutability in Java.", ideal: "Strings in Java are immutable - once created, their value cannot be changed. Any operation that appears to modify a String actually creates a new String object. This provides thread safety, allows String pooling for memory efficiency, and enables safe use as HashMap keys. Use StringBuilder for mutable string operations." },
            { text: "What is the difference between String, StringBuilder, and StringBuffer?", ideal: "String is immutable and thread-safe. StringBuilder is mutable and NOT thread-safe, best for single-threaded string manipulation. StringBuffer is mutable and thread-safe (synchronized), slower than StringBuilder. Use String for constant values, StringBuilder for building strings in loops, StringBuffer only when thread safety is needed." },
            { text: "What are access modifiers in Java?", ideal: "Access modifiers control visibility: 'public' (accessible everywhere), 'private' (only within the same class), 'protected' (same package and subclasses), and default/package-private (same package only). They implement encapsulation by controlling what parts of code can access class members." },
            { text: "Explain the concept of encapsulation.", ideal: "Encapsulation bundles data (fields) and methods that operate on that data within a class, and restricts direct access to some components. Use private fields with public getter/setter methods to control access. This hides internal implementation, protects data integrity, and allows changing implementation without affecting external code." },
            { text: "What is a constructor in Java?", ideal: "A constructor is a special method called when creating an object using 'new'. It has the same name as the class and no return type. Constructors initialize object state. Java provides a default no-argument constructor if you don't define one. You can overload constructors to provide different initialization options." },
            { text: "What is constructor overloading?", ideal: "Constructor overloading means having multiple constructors with different parameter lists in the same class. This allows creating objects in different ways. Example: Person() for default, Person(String name) with name only, Person(String name, int age) with both. Java chooses the constructor based on arguments provided." },
            { text: "What is the 'this' keyword in Java?", ideal: "The 'this' keyword refers to the current object instance. It's used to: (1) distinguish between instance variables and parameters with the same name, (2) call other constructors in the same class using this(), (3) pass the current object as a parameter, (4) return the current object from a method." },
            { text: "What is method overloading?", ideal: "Method overloading allows multiple methods with the same name but different parameter lists (different number, type, or order of parameters) in the same class. The return type alone is NOT enough to overload. Java determines which method to call based on the arguments at compile time (compile-time polymorphism)." },
            { text: "What is the difference between == and equals()?", ideal: "== compares object references (memory addresses) - checks if two references point to the same object. equals() compares object content/values - checks if two objects are logically equal. For primitives, use ==. For objects like String, use equals(). You can override equals() to define custom equality logic." },
            { text: "Explain the if-else statement in Java.", ideal: "if-else controls program flow based on boolean conditions. Syntax: if (condition) { code } else if (condition2) { code } else { code }. Only the first true condition executes. Use for binary decisions or multiple conditions. For single statements, braces are optional but recommended for clarity." },
            { text: "What is a switch statement and when to use it?", ideal: "Switch statement selects one of many code blocks to execute based on a variable's value. Works with byte, short, int, char, String, and enums. More readable than multiple if-else for checking one variable against many values. Remember to use 'break' to prevent fall-through, or use fall-through intentionally." },
            { text: "Explain the for loop in Java.", ideal: "For loop repeats code a specific number of times. Syntax: for (initialization; condition; update) { code }. Example: for (int i = 0; i < 10; i++) iterates 10 times. Initialization runs once, condition checks before each iteration, update runs after each iteration. Use when you know iteration count." },
            { text: "What is a while loop?", ideal: "While loop repeats code while a condition is true. Syntax: while (condition) { code }. The condition is checked before each iteration. If initially false, the loop never executes. Use when iteration count is unknown and depends on a condition. Be careful to avoid infinite loops by ensuring the condition eventually becomes false." },
            { text: "What is the difference between while and do-while loops?", ideal: "while checks the condition before executing the loop body - may execute 0 times. do-while checks the condition after executing the loop body - always executes at least once. Syntax: do { code } while (condition); Use do-while when you need the code to run at least once, like menu-driven programs." },
            { text: "What is an array in Java?", ideal: "An array is a fixed-size container that holds elements of the same type. Declare: int[] arr = new int[5]; or int[] arr = {1,2,3,4,5};. Arrays are zero-indexed. Access elements with arr[index]. Arrays have a fixed length (arr.length). Use arrays for fixed-size collections of the same type." },
            { text: "How do you iterate through an array?", ideal: "Three ways: (1) Traditional for loop: for (int i = 0; i < arr.length; i++) { arr[i] }, (2) Enhanced for loop: for (int element : arr) { element }, (3) Using Arrays utility: Arrays.stream(arr).forEach(). Enhanced for loop is simpler for read-only iteration. Use traditional for when you need the index." },
            { text: "What is a multidimensional array?", ideal: "A multidimensional array is an array of arrays. 2D array: int[][] matrix = new int[3][4]; creates 3 rows and 4 columns. Access: matrix[row][col]. Can have jagged arrays where rows have different lengths. Use for matrices, grids, or tabular data. Iterate with nested loops." },
            { text: "What is the difference between break and continue?", ideal: "break exits the loop entirely and continues execution after the loop. continue skips the current iteration and moves to the next iteration. Example: in a loop from 1-10, if (i == 5) break; stops at 5, but if (i == 5) continue; skips 5 and continues with 6. Both work in for, while, and do-while loops." },
            { text: "What is a class in Java?", ideal: "A class is a blueprint or template for creating objects. It defines properties (fields/variables) and behaviors (methods) that objects of that class will have. Example: class Car { String color; void drive() {} }. Classes implement OOP concepts like encapsulation, inheritance, and polymorphism. Every Java program has at least one class." },
            { text: "What is an object in Java?", ideal: "An object is an instance of a class created using the 'new' keyword. It has state (field values) and behavior (methods). Example: Car myCar = new Car(); creates a Car object. Each object has its own copy of instance variables but shares methods. Objects are stored in heap memory." },
            { text: "What is inheritance in Java?", ideal: "Inheritance allows a class (subclass/child) to inherit fields and methods from another class (superclass/parent) using 'extends'. It promotes code reuse and establishes an 'is-a' relationship. Example: class Dog extends Animal. Java supports single inheritance only (one parent class). Use super keyword to access parent class members." },
            { text: "What is the 'super' keyword?", ideal: "The 'super' keyword refers to the parent class. It's used to: (1) call parent class methods: super.methodName(), (2) call parent class constructor: super() or super(args) - must be first line in child constructor, (3) access parent class fields: super.fieldName. Use when child class overrides parent methods but still needs parent functionality." },
            { text: "What is method overriding?", ideal: "Method overriding occurs when a subclass provides a specific implementation for a method already defined in its parent class. The method must have the same name, return type, and parameters. Use @Override annotation to ensure you're actually overriding. This is runtime polymorphism - the JVM decides which method to call based on the object type." },
            { text: "What is polymorphism in Java?", ideal: "Polymorphism means 'many forms' - one interface, multiple implementations. Two types: (1) Compile-time polymorphism: method overloading, (2) Runtime polymorphism: method overriding. Example: Animal a = new Dog(); a.sound(); calls Dog's sound() method. Allows writing flexible code that works with parent class references but executes child class behavior." },
            { text: "What is an abstract class?", ideal: "An abstract class is declared with 'abstract' keyword and cannot be instantiated. It can have abstract methods (no body) that must be implemented by subclasses, and concrete methods (with body). Use when classes share common code but some methods need different implementations. Example: abstract class Shape { abstract void draw(); }" },
            { text: "What is an interface in Java?", ideal: "An interface is a contract that defines methods a class must implement, declared with 'interface' keyword. All methods are implicitly public and abstract (before Java 8). A class implements an interface using 'implements'. Classes can implement multiple interfaces (multiple inheritance of type). Use for defining capabilities that unrelated classes can share." },
            { text: "What is the difference between abstract class and interface?", ideal: "Abstract classes can have constructors, instance variables, and concrete methods; use 'extends' (single inheritance). Interfaces cannot have constructors or instance variables (before Java 8), all methods are abstract; use 'implements' (multiple inheritance). Use abstract class for 'is-a' relationships with shared code, interface for 'can-do' capabilities." },
            { text: "What is a package in Java?", ideal: "A package is a namespace that organizes classes and interfaces into groups. It prevents naming conflicts and controls access. Declare with 'package' keyword at the top of file. Import with 'import packageName.ClassName;'. Example: java.util contains utility classes. Use packages to organize large projects logically." },
            { text: "What is the import statement?", ideal: "The import statement allows you to use classes from other packages without fully qualified names. Example: import java.util.ArrayList; lets you use ArrayList instead of java.util.ArrayList. Use import java.util.*; to import all classes from a package (not recommended for large packages). Import is a compile-time feature." },
            { text: "What is exception handling in Java?", ideal: "Exception handling manages runtime errors using try-catch-finally blocks. try contains code that might throw an exception, catch handles specific exceptions, finally executes regardless of exceptions (cleanup code). Example: try { code } catch (Exception e) { handle } finally { cleanup }. This prevents program crashes and allows graceful error recovery." },
            { text: "What is the difference between checked and unchecked exceptions?", ideal: "Checked exceptions (IOException, SQLException) must be caught or declared with 'throws' - compiler enforces this. Unchecked exceptions (NullPointerException, ArrayIndexOutOfBoundsException) extend RuntimeException and don't require handling. Checked exceptions are for recoverable conditions, unchecked for programming errors. Use checked for expected errors, unchecked for bugs." },
            { text: "What is the finally block?", ideal: "The finally block executes after try-catch regardless of whether an exception occurred or was caught. It's used for cleanup code like closing files or database connections. Syntax: try { } catch { } finally { cleanup }. Finally executes even if there's a return statement in try or catch. Only system exit or fatal errors prevent finally execution." },
            { text: "What is the throw keyword?", ideal: "The 'throw' keyword explicitly throws an exception. Syntax: throw new ExceptionType('message');. Use to create and throw custom exceptions or re-throw caught exceptions. Example: if (age < 0) throw new IllegalArgumentException('Age cannot be negative');. This allows you to signal errors in your code." },
            { text: "What is the throws keyword?", ideal: "The 'throws' keyword in method signature declares that a method might throw certain exceptions, passing the responsibility to the caller. Syntax: void method() throws IOException, SQLException { }. Required for checked exceptions. The caller must handle these exceptions with try-catch or declare them with throws. This documents potential errors." },
            { text: "What is a static variable?", ideal: "A static variable (class variable) belongs to the class, not instances. It's shared among all objects of the class. Declared with 'static' keyword. Accessed using ClassName.variableName. Initialized once when class loads. Use for constants (with final) or data shared across all instances like counters." },
            { text: "What is a static method?", ideal: "A static method belongs to the class, not instances. It can be called without creating an object using ClassName.methodName(). It can only access static variables and call other static methods directly. Cannot use 'this' or 'super'. Use for utility methods that don't need instance data, like Math.sqrt()." },
            { text: "What is the final keyword?", ideal: "The 'final' keyword makes things unchangeable: (1) final variable: constant, cannot be reassigned, (2) final method: cannot be overridden by subclasses, (3) final class: cannot be inherited. Use final for constants (static final), preventing inheritance, or ensuring methods aren't overridden. Example: final double PI = 3.14159;" },
            { text: "What is a static block?", ideal: "A static block (static initializer) executes once when the class is loaded, before any object creation or static method calls. Syntax: static { initialization code }. Use for complex static variable initialization that requires logic. Multiple static blocks execute in order. Runs before constructors." },
            { text: "What is the difference between ArrayList and Array?", ideal: "Arrays have fixed size, ArrayList is dynamic (resizable). Arrays can hold primitives and objects, ArrayList only holds objects (uses autoboxing for primitives). Arrays use arr[index], ArrayList uses get(index) and set(index, value). ArrayList provides methods like add(), remove(), contains(). Use arrays for fixed size, ArrayList for dynamic collections." },
            { text: "How do you create an ArrayList?", ideal: "Import java.util.ArrayList, then create: ArrayList<Type> list = new ArrayList<>();. Example: ArrayList<String> names = new ArrayList<>();. Use generics <Type> to specify element type. Add elements: list.add(element), access: list.get(index), remove: list.remove(index), size: list.size(). ArrayList is part of Java Collections Framework." },
            { text: "What is the difference between = and == operators?", ideal: "= is the assignment operator that assigns a value to a variable. Example: int x = 5; assigns 5 to x. == is the equality comparison operator that checks if two values are equal, returns boolean. Example: if (x == 5) checks if x equals 5. Don't confuse them - using = in conditions is a common bug." },
            { text: "What are logical operators in Java?", ideal: "Logical operators work with boolean values: && (AND) - true if both are true, || (OR) - true if at least one is true, ! (NOT) - inverts boolean value. && and || use short-circuit evaluation - if first operand determines result, second isn't evaluated. Example: if (x > 0 && y > 0) checks both conditions." },
            { text: "What is the ternary operator?", ideal: "The ternary operator is a shorthand for if-else. Syntax: condition ? valueIfTrue : valueIfFalse. Example: int max = (a > b) ? a : b; assigns the larger value to max. It's an expression that returns a value. Use for simple conditional assignments. Don't nest ternary operators - reduces readability." },
            { text: "What is type casting in Java?", ideal: "Type casting converts one data type to another. Widening (implicit): smaller to larger type, automatic (int to double). Narrowing (explicit): larger to smaller type, requires cast operator (double to int). Example: int x = (int) 3.14; casts double to int, losing decimal. For objects, cast between parent and child classes." },
            { text: "What is autoboxing and unboxing?", ideal: "Autoboxing automatically converts primitives to wrapper objects (int to Integer). Unboxing converts wrapper objects to primitives (Integer to int). Example: Integer obj = 5; (autoboxing), int num = obj; (unboxing). This happens automatically in assignments, method calls, and collections. Be careful with null values - unboxing null throws NullPointerException." },
            { text: "What is a NullPointerException?", ideal: "NullPointerException occurs when you try to use a null reference as if it were an object. Common causes: calling methods on null (obj.method()), accessing null array (arr[0]), accessing null fields. Prevent by: checking if (obj != null), using Optional, initializing variables properly. It's the most common runtime exception in Java." }
        ]
    },
    {
        id: 'python-basic',
        title: 'Python Programming (Basic)',
        description: 'Python Fundamentals, Basic OOP, and Core Syntax.',
        icon: 'Code',
        questions: [
            { text: "What is Python and why is it popular?", ideal: "Python is a high-level, interpreted, dynamically-typed programming language known for its simple, readable syntax. It's popular because: easy to learn, versatile (web, data science, AI, automation), large standard library, huge community, cross-platform. Python emphasizes code readability with significant whitespace (indentation). Used by Google, Netflix, NASA." },
            { text: "What are Python's key features?", ideal: "Key features: (1) Easy to learn and read with clean syntax, (2) Interpreted - no compilation needed, (3) Dynamically typed - no need to declare variable types, (4) Object-oriented and functional programming support, (5) Extensive standard library, (6) Cross-platform, (7) Free and open-source, (8) Great for rapid development, (9) Strong community support." },
            { text: "What is the difference between Python 2 and Python 3?", ideal: "Python 3 is the current version with improvements. Key differences: print is a function in Python 3 (print() vs print), division returns float in Python 3 (5/2 = 2.5 vs 2), strings are Unicode by default in Python 3, range() returns iterator in Python 3. Python 2 reached end-of-life in 2020. Always use Python 3 for new projects." },
            { text: "What are variables in Python?", ideal: "Variables store data values. Python has no variable declaration command - variables are created when you assign a value. Example: x = 5, name = 'John'. Python is dynamically typed - variable type is determined at runtime. You can change variable type: x = 5, then x = 'hello'. Use descriptive names with underscores: user_name." },
            { text: "What are Python's basic data types?", ideal: "Basic types: (1) int - integers (5, -3), (2) float - decimals (3.14, -0.5), (3) str - strings ('hello', \"world\"), (4) bool - boolean (True, False), (5) None - null value. Check type with type(variable). Python automatically determines type. Convert between types: int('5'), str(5), float('3.14')." },
            { text: "What is the difference between list and tuple?", ideal: "Lists are mutable (can be changed) and use square brackets: [1, 2, 3]. Tuples are immutable (cannot be changed) and use parentheses: (1, 2, 3). Lists have methods like append(), remove(). Tuples are faster and use less memory. Use lists for collections that change, tuples for fixed data like coordinates." },
            { text: "How do you create and use a list?", ideal: "Create: my_list = [1, 2, 3, 'hello']. Access: my_list[0] (first element). Modify: my_list[0] = 10. Add: my_list.append(4). Remove: my_list.remove(2). Length: len(my_list). Slice: my_list[1:3]. Lists can hold different types. Use for dynamic collections of items." },
            { text: "What is a dictionary in Python?", ideal: "Dictionary stores key-value pairs using curly braces: {'name': 'John', 'age': 25}. Access values by key: person['name']. Add/modify: person['city'] = 'NYC'. Remove: del person['age']. Check key: 'name' in person. Get keys: person.keys(), values: person.values(). Dictionaries are unordered (before Python 3.7) and mutable." },
            { text: "What is a set in Python?", ideal: "Set is an unordered collection of unique elements using curly braces: {1, 2, 3}. Automatically removes duplicates. Add: my_set.add(4). Remove: my_set.remove(2). Operations: union (|), intersection (&), difference (-). Use for membership testing and removing duplicates. Sets are mutable, frozenset is immutable." },
            { text: "What are strings in Python?", ideal: "Strings are sequences of characters in quotes: 'hello' or \"hello\" or '''multi-line'''. Immutable - cannot change characters. Access: s[0] (first char). Slice: s[1:4]. Concatenate: s1 + s2. Methods: s.upper(), s.lower(), s.strip(), s.split(), s.replace(). Format: f'Hello {name}' or 'Hello {}'.format(name)." },
            { text: "How do you format strings in Python?", ideal: "Three ways: (1) f-strings (Python 3.6+): f'Hello {name}, you are {age}', (2) format(): 'Hello {}, you are {}'.format(name, age), (3) % operator: 'Hello %s, you are %d' % (name, age). f-strings are fastest and most readable. Use for embedding variables in strings." },
            { text: "What are Python operators?", ideal: "Arithmetic: +, -, *, / (division), // (floor division), % (modulo), ** (power). Comparison: ==, !=, <, >, <=, >=. Logical: and, or, not. Assignment: =, +=, -=, *=, /=. Membership: in, not in. Identity: is, is not. Bitwise: &, |, ^, ~, <<, >>." },
            { text: "What is the difference between == and is?", ideal: "== compares values (equality). 'is' compares object identity (same memory location). Example: a = [1,2], b = [1,2], c = a. a == b is True (same values), a is b is False (different objects), a is c is True (same object). Use == for value comparison, 'is' for None checks: if x is None." },
            { text: "What are conditional statements in Python?", ideal: "if-elif-else controls flow based on conditions. Syntax: if condition: code, elif condition: code, else: code. Python uses indentation (4 spaces) instead of braces. Example: if x > 0: print('positive'), elif x < 0: print('negative'), else: print('zero'). Conditions can use and, or, not." },
            { text: "What is a for loop in Python?", ideal: "for loop iterates over sequences (lists, tuples, strings, range). Syntax: for item in sequence: code. Example: for i in range(5): print(i) prints 0-4. for name in names: print(name) iterates list. Use enumerate() for index: for i, name in enumerate(names). Use for known iterations." },
            { text: "What is the range() function?", ideal: "range() generates sequence of numbers. range(5) = 0,1,2,3,4. range(1, 6) = 1,2,3,4,5. range(0, 10, 2) = 0,2,4,6,8 (start, stop, step). Returns range object (iterator), not list. Convert to list: list(range(5)). Commonly used in for loops: for i in range(10)." },
            { text: "What is a while loop?", ideal: "while loop repeats while condition is true. Syntax: while condition: code. Example: while x < 10: x += 1. Check condition before each iteration. Use when iteration count is unknown. Be careful of infinite loops - ensure condition eventually becomes False. Use break to exit, continue to skip iteration." },
            { text: "What are break and continue statements?", ideal: "break exits the loop entirely. continue skips current iteration and moves to next. Example: for i in range(10): if i == 5: break (stops at 5), if i == 5: continue (skips 5, continues with 6). Both work in for and while loops. Use break for early exit, continue to skip specific iterations." },
            { text: "What is a function in Python?", ideal: "Function is a reusable block of code defined with 'def'. Syntax: def function_name(parameters): code, return value. Example: def add(a, b): return a + b. Call: result = add(5, 3). Functions can have default parameters: def greet(name='Guest'). Use functions to organize code and avoid repetition." },
            { text: "What are function arguments and parameters?", ideal: "Parameters are variables in function definition. Arguments are values passed when calling. Types: (1) Positional: def func(a, b), (2) Keyword: func(a=1, b=2), (3) Default: def func(a, b=10), (4) *args for variable positional args, (5) **kwargs for variable keyword args. Example: def func(*args, **kwargs)." },
            { text: "What is the return statement?", ideal: "return sends a value back from a function and exits the function. Example: def add(a, b): return a + b. Can return multiple values: return x, y (returns tuple). If no return statement, function returns None. Use return to get results from functions. Can have multiple return statements for different conditions." },
            { text: "What are lambda functions?", ideal: "Lambda is an anonymous (unnamed) function defined in one line. Syntax: lambda arguments: expression. Example: square = lambda x: x**2, square(5) returns 25. Used for simple functions, often with map(), filter(), sorted(). Example: sorted(items, key=lambda x: x[1]). Don't use for complex logic - use def instead." },
            { text: "What is list comprehension?", ideal: "List comprehension creates lists in one line. Syntax: [expression for item in iterable if condition]. Example: squares = [x**2 for x in range(10)] creates [0,1,4,9,16,25,36,49,64,81]. With condition: evens = [x for x in range(10) if x % 2 == 0]. More readable and faster than loops for simple transformations." },
            { text: "What are Python modules?", ideal: "Module is a file containing Python code (functions, classes, variables). Import with 'import module_name'. Access: module_name.function(). Import specific items: from module import function. Rename: import module as m. Python has built-in modules (math, random, datetime) and you can create custom modules. Organize code into reusable files." },
            { text: "How do you import modules?", ideal: "Ways to import: (1) import math - use math.sqrt(), (2) from math import sqrt - use sqrt() directly, (3) from math import * - import all (not recommended), (4) import math as m - use m.sqrt(). For custom modules: import my_module. Module must be in same directory or Python path." },
            { text: "What is the difference between append() and extend()?", ideal: "append() adds a single element to the end of a list: [1,2].append(3) = [1,2,3]. extend() adds multiple elements from an iterable: [1,2].extend([3,4]) = [1,2,3,4]. append([3,4]) would create [1,2,[3,4]] (nested list). Use append for single items, extend for merging lists." },
            { text: "What are Python exceptions?", ideal: "Exceptions are errors that occur during execution. Common ones: NameError (undefined variable), TypeError (wrong type), ValueError (wrong value), IndexError (invalid index), KeyError (invalid dict key), ZeroDivisionError. Handle with try-except: try: code, except ExceptionType: handle. Prevents program crashes." },
            { text: "How do you handle exceptions in Python?", ideal: "Use try-except-else-finally. try: risky code, except ExceptionType as e: handle error, else: runs if no exception, finally: always runs (cleanup). Example: try: x = int(input()), except ValueError: print('Invalid'), finally: print('Done'). Can catch multiple exceptions: except (TypeError, ValueError). Use to prevent crashes and handle errors gracefully." },
            { text: "What is the difference between remove(), pop(), and del?", ideal: "remove(value) removes first occurrence of value: [1,2,3,2].remove(2) = [1,3,2]. pop(index) removes and returns item at index: [1,2,3].pop(1) returns 2, list becomes [1,3]. pop() without index removes last item. del list[index] deletes item at index. del can also delete entire list: del list. Use based on whether you need the removed value." },
            { text: "What is a class in Python?", ideal: "Class is a blueprint for creating objects. Define with 'class' keyword. Example: class Dog: def __init__(self, name): self.name = name, def bark(self): print('Woof'). Create object: my_dog = Dog('Buddy'). Classes bundle data (attributes) and functions (methods). Use for organizing related code and creating multiple similar objects." },
            { text: "What is the __init__ method?", ideal: "__init__ is the constructor method called when creating an object. It initializes object attributes. Example: class Person: def __init__(self, name, age): self.name = name, self.age = age. 'self' refers to the instance. Called automatically: p = Person('John', 25). Use to set up initial state of objects." },
            { text: "What is 'self' in Python?", ideal: "'self' represents the instance of the class. It's the first parameter in instance methods. Used to access instance variables and methods: self.name, self.method(). Python passes the instance automatically when calling methods: obj.method() actually calls Class.method(obj). You can name it anything, but 'self' is convention." },
            { text: "What is inheritance in Python?", ideal: "Inheritance allows a class (child) to inherit attributes and methods from another class (parent). Syntax: class Child(Parent). Example: class Dog(Animal). Child can override parent methods and add new ones. Access parent with super(): super().__init__(). Python supports multiple inheritance: class Child(Parent1, Parent2). Use for code reuse and creating hierarchies." },
            { text: "What are Python decorators?", ideal: "Decorators modify or enhance functions without changing their code. They're functions that take a function and return a modified function. Use @decorator syntax. Example: @staticmethod, @property. Common use: logging, timing, authentication. Simple example: @my_decorator, def function(): pass. Advanced topic but important for understanding Python frameworks." },
            { text: "What is the difference between a method and a function?", ideal: "Function is independent, defined with 'def' outside classes. Method is a function defined inside a class and operates on class instances. Methods have 'self' as first parameter. Call function: function(), call method: object.method(). Methods can access instance data via self. All methods are functions, but not all functions are methods." },
            { text: "What are *args and **kwargs?", ideal: "*args allows passing variable number of positional arguments as tuple. **kwargs allows variable keyword arguments as dictionary. Example: def func(*args, **kwargs): print(args, kwargs). Call: func(1, 2, 3, a=4, b=5) prints (1,2,3) and {'a':4, 'b':5}. Use when you don't know how many arguments will be passed." },
            { text: "What is the difference between local and global variables?", ideal: "Local variables are defined inside functions, only accessible within that function. Global variables are defined outside functions, accessible everywhere. Use 'global' keyword to modify global variable inside function: global x, x = 10. Local variables are destroyed when function ends. Avoid global variables when possible - use function parameters and return values." },
            { text: "What are Python file operations?", ideal: "Open file: f = open('file.txt', 'r'). Modes: 'r' (read), 'w' (write), 'a' (append), 'r+' (read+write). Read: f.read(), f.readline(), f.readlines(). Write: f.write('text'). Close: f.close(). Better: use 'with' statement: with open('file.txt', 'r') as f: data = f.read() - automatically closes file." },
            { text: "What is the 'with' statement?", ideal: "'with' statement simplifies resource management (files, connections). It ensures proper cleanup even if errors occur. Syntax: with open('file.txt') as f: data = f.read(). File automatically closes after block. Implements context manager protocol (__enter__ and __exit__). Use for files, database connections, locks. Prevents resource leaks." },
            { text: "What is the difference between shallow and deep copy?", ideal: "Shallow copy (copy.copy()) creates new object but references same nested objects. Deep copy (copy.deepcopy()) creates new object and recursively copies all nested objects. Example: list2 = list1.copy() is shallow - changing nested list affects both. import copy, list2 = copy.deepcopy(list1) is deep - completely independent. Use deep copy for nested structures." },
            { text: "What are Python's built-in functions?", ideal: "Common built-ins: print(), input(), len(), type(), range(), int(), str(), float(), list(), dict(), set(), sum(), max(), min(), sorted(), enumerate(), zip(), map(), filter(), all(), any(), abs(), round(), help(). These are always available without importing. Use help(function) to learn about any function." },
            { text: "What is the enumerate() function?", ideal: "enumerate() adds counter to iterable, returns enumerate object of (index, value) tuples. Example: for i, name in enumerate(['Alice', 'Bob']): print(i, name) prints 0 Alice, 1 Bob. Start from different index: enumerate(list, start=1). Use when you need both index and value in loops instead of range(len())." },
            { text: "What is the zip() function?", ideal: "zip() combines multiple iterables into tuples. Example: list(zip([1,2,3], ['a','b','c'])) = [(1,'a'), (2,'b'), (3,'c')]. Stops at shortest iterable. Use in loops: for num, letter in zip(numbers, letters). Unzip with zip(*zipped). Use to iterate multiple lists simultaneously." },
            { text: "What is the map() function?", ideal: "map() applies a function to all items in iterable. Returns map object (iterator). Example: list(map(str, [1,2,3])) = ['1','2','3']. With lambda: list(map(lambda x: x**2, [1,2,3])) = [1,4,9]. More readable than loops for simple transformations. List comprehension is often preferred: [x**2 for x in [1,2,3]]." },
            { text: "What is the filter() function?", ideal: "filter() filters items based on a function that returns True/False. Returns filter object. Example: list(filter(lambda x: x > 0, [-1,0,1,2])) = [1,2]. With function: def is_even(x): return x % 2 == 0, list(filter(is_even, [1,2,3,4])) = [2,4]. List comprehension alternative: [x for x in list if x > 0]." },
            { text: "What are Python comments?", ideal: "Single-line comments use #: # This is a comment. Multi-line comments use triple quotes: '''This is a multi-line comment''' or \"\"\"comment\"\"\". Comments are ignored by Python. Use to explain code, disable code temporarily, document functions. Good comments explain WHY, not WHAT (code shows what). Keep comments updated with code." },
            { text: "What is None in Python?", ideal: "None is Python's null value, represents absence of value. It's a singleton object of type NoneType. Functions without return statement return None. Check with 'is': if x is None (not ==). Common uses: default parameter values, initializing variables, indicating no result. None is falsy in boolean context." },
            { text: "What are Python's boolean values?", ideal: "Boolean values are True and False (capitalized). Result from comparisons: 5 > 3 is True. Falsy values (evaluate to False): False, None, 0, 0.0, '', [], {}, (). Everything else is truthy. Logical operators: and, or, not. Use in conditions: if my_list (checks if list is not empty)." },
            { text: "What is the pass statement?", ideal: "pass is a null statement that does nothing. It's a placeholder when syntax requires a statement but you don't want to execute code. Example: if x > 0: pass (do nothing). Use in empty functions: def func(): pass, empty classes: class MyClass: pass. Useful during development to avoid syntax errors while writing code structure." },
            { text: "What is string slicing?", ideal: "Slicing extracts parts of strings (or lists). Syntax: string[start:stop:step]. Example: s = 'Python', s[0:3] = 'Pyt', s[2:] = 'thon', s[:4] = 'Pyth', s[::2] = 'Pto' (every 2nd char), s[::-1] = 'nohtyP' (reverse). Negative indices: s[-1] = 'n' (last char). Slicing doesn't modify original string." }
        ]
    }
];

// Shuffle helper
const _shuffle = (arr) => {
    const a = [...arr];
    for (let i = a.length - 1; i > 0; i--) {
        const j = Math.floor(Math.random() * (i + 1));
        [a[i], a[j]] = [a[j], a[i]];
    }
    return a;
};

// Helper to get random questions with smart selection (avoids repeats across sessions)
// Guarantees: questions in session N are NEVER the same as questions in session N-1
export const getRandomQuestions = (domainId, count = 5) => {
    const domain = domains.find(d => d.id === domainId) || domains[0];
    const allQuestions = [...domain.questions];

    // Key 1: Full history of all asked questions (rolling, up to full pool size)
    const historyKey = `asked_questions_${domainId}`;
    // Key 2: The LAST session's questions — always excluded from the next session
    const lastSessionKey = `last_session_${domainId}`;

    let askedTexts = [];
    let lastSessionTexts = [];
    try {
        askedTexts = JSON.parse(localStorage.getItem(historyKey) || '[]');
        lastSessionTexts = JSON.parse(localStorage.getItem(lastSessionKey) || '[]');
    } catch {
        askedTexts = [];
        lastSessionTexts = [];
    }

    // Build the candidate pool: prefer questions NOT in the last session
    const notInLastSession = allQuestions.filter(q => !lastSessionTexts.includes(q.text));

    // If excluding last session leaves enough candidates, use that pool; otherwise use all
    const candidatePool = notInLastSession.length >= count ? notInLastSession : allQuestions;

    // Within candidate pool: prioritise questions not yet seen in ANY prior session
    const freshUnasked = _shuffle(candidatePool.filter(q => !askedTexts.includes(q.text)));
    const seenBefore   = _shuffle(candidatePool.filter(q =>  askedTexts.includes(q.text)));

    // If all candidates have been seen, reset history for candidate pool and try again
    if (freshUnasked.length === 0 && seenBefore.length === 0) {
        localStorage.removeItem(historyKey);
        localStorage.removeItem(lastSessionKey);
        return getRandomQuestions(domainId, count);
    }

    // Pick: fresh questions first, fill with previously-seen if needed
    const selected = [...freshUnasked.slice(0, count)];
    if (selected.length < count) {
        selected.push(...seenBefore.slice(0, count - selected.length));
    }

    // Final shuffle so question order is also unpredictable
    const finalSelected = _shuffle(selected).slice(0, count);
    const selectedTexts = finalSelected.map(q => q.text);

    // Save: update rolling history + save this session so next session can exclude it
    const newAskedTexts = [...askedTexts, ...selectedTexts];
    const maxTracked = allQuestions.length;
    try {
        localStorage.setItem(historyKey, JSON.stringify(newAskedTexts.slice(-maxTracked)));
        localStorage.setItem(lastSessionKey, JSON.stringify(selectedTexts));
    } catch { /* localStorage full, ignore */ }

    return finalSelected;
};
